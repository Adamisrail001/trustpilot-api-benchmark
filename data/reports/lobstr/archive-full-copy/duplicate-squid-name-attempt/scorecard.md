# Lobstr.io Trustpilot Reviews — Provisional Scorecard (/10 rubric)

## Result: No score assigned

Per the standing project rule ("do not assign a fabricated score if the benchmark is invalid or contaminated"), no `/10` score is produced. This attempt collected zero reviews due to a configuration-level error (squid-name collision — see `error-investigation.md`), not a demonstrated Lobstr.io capability result. Scoring against zero real data would be fabrication, not measurement.

A score will be produced once a run actually executes against the 5 approved domains.
