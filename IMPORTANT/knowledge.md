# Trustpilot Review-Scraping API Benchmark — Knowledge Base

> This document is the verified factual record for the Trustpilot API benchmark and the article built on it. Every claim and figure below must trace to logged benchmark evidence — nothing is inferred, estimated, or assumed. Evidence status is marked throughout: ✅ **CAPTURED** (complete, traceable evidence), ⚠️ **PARTIAL** (incomplete validation), ❌ **MISSING** / **Not tested** (no evidence — never softened into an estimate), ❓ **UNCLEAR** (provenance or interpretation unconfirmed), 🔴 **DISCREPANCY** (sources conflict, not silently resolved).
>
> 🔴 **2026-09-11: all numeric scoring removed from this document.** This document previously carried a full point-based scoring rubric (per-criterion sub-scores, /10 normalizations, mean-of-N overall totals) layered on top of the raw facts. Per explicit instruction, that scoring layer has been stripped out entirely — this document now records only facts, figures, and evidence, not rankings or point values. The scoring rubric itself still lives in `criteria.md`/`methodology.md` (untouched by this change) if a scored comparison is ever needed again; nothing about the underlying evidence changed, only whether this document computes and states a score from it.
>
> **Scope of the benchmark (all 5 providers):** DataForSEO, Outscraper, OpenWeb Ninja, Apify, Lobstr were each run once, on the same 5 fixed Trustpilot business domains — `www.thepearlsource.com`, `www.shein.com`, `temu.com`, `www.aliexpress.com`, `thehalara.com` — each targeting 200 reviews (1,000 requested per provider). A separate isolated Lobstr test targeted `www.thepearlsource.com` alone, later verified at a 2,000-review target with 2,000/2,000 unique valid reviews and 0 duplicates (2026-08-13 — see Live verification pass below). 🔴 An earlier 1,000-review version of this same isolated test is referenced elsewhere but has no surviving raw evidence anywhere in this project — treat only the 2,000-review figure as confirmed.
>
> **Live verification pass (2026-08-13):** DataForSEO, Apify, and Outscraper's 5-domain benchmarks were rerun live against real accounts, and Lobstr's isolated single-business test was extended to a 2,000-review target on `www.thepearlsource.com` (still isolated on its own dedicated squid, still separate from the 5-domain comparison). OpenWeb Ninja was not rerun (no quota-remaining figure was available). Every figure below reflects this live pass where one exists; where it doesn't, the prior evidence is marked as such.
>
> 🔴 **New: five-domain multi-task test (2026-09-04).** A third, distinct Lobstr test: all 5 standard benchmark businesses added as 5 tasks in **one** dedicated squid, `max_results`/`max_unique_results_per_run` raised to 1,000/task (5,000 run cap), started as **one** paid run. Result: **5,000/5,000 unique valid reviews, 0 duplicates, 0 unattributed, all 5 domains exactly 1,000/1,000, 0 error events.** This is a different axis from the 2,000-review test above — that one pushed depth on a *single* business past 200; this one holds depth at 1,000/business but proves multiple businesses can be collected in one squid/one run without a run-level cap, retry, or attribution failure. Full evidence (unlike the lost 1,000-review isolated test) is committed at `data/lobstr/five-domain-multi-task-test-2026-09-04/` — see `summary.json`. See sections 1, 2, 3, and 4 below for the figures pulled from it.
>
> 🔴 **New: Apify alt-actor (memo23) five-domain bulk test, paid account (2026-09-16) — the direct counterpart to the Lobstr test immediately above.** Same 5 benchmark businesses, same 1,000-reviews-per-business target (5,000 total), run against `memo23/trustpilot-scraper-ppe` — **not** `automation-lab/trustpilot`, the Actor actually scored throughout Sections 1–6 of this document (confirmed hard-capped at exactly 200/business, see §4a) — from an Apify account newly upgraded to a **paid** plan. Result: **5,000/5,000 unique valid reviews, 0 duplicates, all 5 domains correctly attributed and re-validated against the now-confirmed item schema.** This is the first time any Apify actor in this project has matched Lobstr's 5,000-review multi-business volume. It does **not** overturn Lobstr's lead — see §4a and the Final Summary for the full reasoning — because it required a different, unscored third-party actor, a paid-plan upgrade (the identical actor returned only 50/1,000 on a free account 13 days earlier), and a higher measured cost per 1,000 reviews. Evidence: `outputs/apify-alt-actor-five-domain-bulk-test/summary.json`, `validated-report.json`. 🔴 **Corrected same day, follow-up test:** an initial 5-separate-runs architecture was used here only because memo23's multi-URL-in-one-run behavior was untested; a same-day follow-up (`outputs/apify-alt-actor-single-run-multi-domain-test/summary.json`) confirmed a single run with all 5 `startUrls` also works, delivering the identical 5,000/5,000 result in **~182s — faster than Lobstr's 398.6s**, not slower as first written up here. See §4a for the full correction. **This document now treats every "who wins on volume" question as a two-contender comparison — Lobstr vs. Apify (via memo23) — since these are the only two providers confirmed able to exceed 200 reviews/business at all; DataForSEO, Outscraper, and OpenWeb Ninja are excluded from that specific question (see §4a).**
>
> 🔴 **Actor identity note, added 2026-09-16, superseded same day (see next note).** Originally stated: wherever this document says "Apify" for the Reliability/Cost/Speed/Data Quality sections (§1–§3, §6), it means `automation-lab/trustpilot`, and `memo23` exists only in §4a. That framing is now reversed per explicit instruction — see below. Kept for provenance, not current guidance.
>
> 🔴 **Reversed 2026-09-16: `memo23/trustpilot-scraper-ppe` is now the primary Actor this document means by "Apify," across all sections, matching the published article.** `automation-lab/trustpilot` — Apify's own official, dedicated Trustpilot Actor — is documented in full throughout §1–§6 and never deleted (hard-capped at 200/business, 80% silent-failure rate, $0.5812/1,000), but it is now the clearly-labeled **secondary/context** reference: the reason a different Actor was needed, not the headline figure. Every cross-provider summary table in §1 (Reliability), §2 (Cost), §3 (Speed), §4a (Data Coverage), and §6 (Data Quality) lists memo23's row ahead of automation-lab's for this reason. When reading any bare "Apify" mention in prose within a per-provider `### Apify` subsection written before 2026-09-16, assume it refers to `automation-lab/trustpilot` unless the surrounding text says otherwise — those subsections are historical evidence logs and are not reordered/rewritten, only corrected and supplemented in place, per this document's standing convention.
> **Ground truth exists for exactly 1 of the 5 domains** (`www.thepearlsource.com`, a manually-verified 20-row sample). SHEIN, Temu, AliExpress, and Halara have **no ground truth at all**. Every accuracy/field-coverage figure below is valid only for `www.thepearlsource.com` and must never be read as representative of the other four domains — this is restated in Section 6 rather than assumed silently.
>
> **Rubric authorship flag:** `criteria.md` (the scoring rubric formerly applied on top of this document's facts, now no longer used here — see note above) is headed "Owner: lobstr.io content team." Lobstr is also one of the five evaluated providers. Both source projects flag this explicitly; neither resolves it. Treat any Lobstr-favorable figure in this document with that in mind, even with the scoring layer removed.

---

## Article Meta

- **Primary SEO keyword:** **"trustpilot reviews api"** — writer-confirmed.
- **Full roster tested:** Apify, OpenWeb Ninja, DataForSEO, Outscraper, Lobstr (Trustpilot's own official API was never tested — see "Eliminations (E1–E6)" below).

---

## Benchmark Methodology Record

- 🔴 **Test window (corrected):** not a single session — the raw evidence dates span roughly a week: DataForSEO's committed run is dated 2026-07-21 (its only run — no archived/committed split exists for this provider); Apify and OpenWeb Ninja's original successful runs 2026-07-22; and Apify's committed evidence / Lobstr's isolated test / OpenWeb Ninja's failure / **Outscraper's committed run** all 2026-07-29. Outscraper also has a separate **archived** run dated 2026-07-21 — that earlier date belongs to the archived evidence, not the committed run, which was previously misgrouped with DataForSEO here. A further live-verification pass ran 2026-08-13 (see the header note above).
- **Shared script template:** one benchmark script template adapted per provider's auth/request format — DataForSEO's port is the weakest/least mature.
- 🔴 **What was logged (corrected):** raw request/response captures, timings, error/cost/pagination reports per provider — uneven: DataForSEO is missing **2** of the standard analysis files (error, pagination reports) every other provider has. Its `cost-report.json` was previously also missing but was backfilled 2026-07-24 — regenerated from the real `cost` field already present in each raw `task_post` response (no new measurement taken), per the file's own `note_on_generation`.
- 🔴 **Public repo URL — corrected 2026-08-20, no longer missing.** Confirmed live and public: `https://github.com/Adamisrail001/trustpilot-api-benchmark` (direct fetch returned 200). Spot-checked individual evidence files also resolve, e.g. `DATA/ground-truth/thepearlsource-sample.json`, `SCRIPTS/outscraper/single_domain_test.py`, `SCRIPTS/apify/single_domain_test.py`. The local `main` branch matches `origin/main` (no divergence), so the pushed repo reflects current evidence. Note: `outputs/` is gitignored (per `.gitignore`), so single-domain-test result files (e.g. `outputs/*-single-domain-test/summary.json`) are **not** publicly reachable — only committed evidence under `DATA/` and `SCRIPTS/` is. Any published article can now link directly to this repo; reproducibility claims in the FAQ should be updated accordingly.

---

## Eliminations (E1–E6)

🔴 **Corrected — this was previously understated as "PARTIAL... rest remain open pending evidence."** That's wrong: all five benchmarked providers have complete, dedicated elimination assessments (all six criteria each), not just the one Trustpilot exclusion below.

**Trustpilot's official API — the one exclusion:**

| API | Criterion failed | One-line reason | Evidence |
|---|---|---|---|
| Trustpilot Official API (Business Units / Product Reviews / Service Reviews) + Data Solutions API | **E1 — No self-serve access** | Neither the standard Business API nor the cross-business Data Solutions API offers instant self-serve access — both require Trustpilot-side setup (API module access / waitlist) before a working key can be obtained. Excluded during pre-screening; no live access was ever obtained and no benchmark was run against it. | Writer-confirmed + [developers.trustpilot.com](https://developers.trustpilot.com/introduction) |

**All 5 benchmarked providers — not eliminated, PASS on all 6 criteria (sourced, per-provider `elimination-assessment.md` files):**

| Provider | E1 | E2 | E3 | E4 | E5 | E6 | Evidence |
|---|---|---|---|---|---|---|---|
| Apify | PASS | PASS | PASS | PASS | PASS | PASS | `DATA/apify/reports/archive-full-copy/2026-07-22-run/elimination-assessment.md` |
| DataForSEO | PASS | PASS | PASS | PASS | PASS | PASS | `DATA/dataforseo/reports/dataforseo-elimination-assessment.md` |
| Outscraper | PASS | PASS | PASS | PASS | PASS | PASS | `DATA/outscraper/reports/archive-full-copy/2026-07-21-run/elimination-assessment.md` |
| OpenWeb Ninja | PASS | PASS | PASS | **NOT eliminated, but flagged** (this account's active "Basic" plan isn't in the published rate card — a cost-transparency gap, not an elimination) | PASS | PASS | `DATA/openwebninja/reports/archive-full-copy/2026-07-22-run/elimination-assessment.md` |
| Lobstr | PASS | PASS | **PASS** — 🔴 previously flagged gap (`/v1/results` `limit` documented as configurable but silently capped at 10) **retracted 2026-09-01**: root cause was a wrong query parameter name (`limit`, which doesn't control page size at all), not a Lobstr platform behavior gap — the real parameter, `page_size`, works correctly and isn't capped. See `data/lobstr/analysis/page_size-parameter-correction.md`. | PASS | PASS | PASS | `DATA/lobstr/reports/archive-full-copy/2026-07-22-run/elimination-assessment.md` |

⚠️ **MISSING: which other tools were discovered pre-benchmark and why each was excluded — ask writer | needs research.** No list, name, or evidence of these tools exists anywhere in the project. This is the only genuinely open item in this section — all 5 tested providers' own elimination status is fully resolved above.

---

## Official API Deep-Dive (Trustpilot)

- **What it actually offers:** Trustpilot exposes three official APIs — Business Units API, Product Reviews API, Service Reviews API — plus a separate **Data Solutions API** that can return review data across businesses (not just one business's own reviews). [Trustpilot developer docs](https://developers.trustpilot.com/)
- **Data Solutions scope, confirmed via direct fetch (2026-08-20):** [`developers.trustpilot.com/data-solutions-get-started`](https://developers.trustpilot.com/data-solutions-get-started) states verbatim: "The Data Solutions API gives you access to Trustpilot's global database of business profiles and consumer reviews." Endpoints include "Get business unit details" (TrustScore, star rating, category, number of reviews) and "Get service reviews" (full text and details of a business's reviews).
- **Data Solutions waitlist, confirmed via the same page (2026-08-20):** verbatim: "To sign up for a Data Solutions product, register your interest on the Data Solutions waitlist. A member of the team will contact you with the next steps to set up your account." This is a direct-doc citation for the waitlist claim in the E1 elimination reasoning above, which previously rested only on writer-confirmation.
- **Access model:** requires a Trustpilot for Business account with **API module** access via the Developer Portal. Public endpoints authenticate with a simple `apikey` header; private endpoints (and the Data Solutions API) require full OAuth 2.0 + Trustpilot-side setup (waitlist/approval) — writer-confirmed this is not self-serve for either path. [Authentication overview](https://developers.trustpilot.com/authentication/) · [Authorization code grant](https://developers.trustpilot.com/grant-type-auth-code/)
- **Public Business Units API note:** returns reviews via `pageToken` pagination but excludes customer email/order ID; the private/OAuth version includes those fields. [Business Units API](https://developers.trustpilot.com/business-units-api)
- **Pricing:** ⚠️ MISSING — unconfirmed. No direct billing evidence exists; do not claim paid pricing without it.
- **THE wall:** not a business's-own-data limitation (the Data Solutions API can cross businesses) — the actual wall is **non-self-serve access**: both the standard Business API and the Data Solutions API require Trustpilot-side setup/waitlist before a working key can be obtained, which fails this benchmark's E1 self-serve eligibility requirement.
- **Live access / code example:** ❌ Not obtained. No working access was ever secured; no live benchmark was run. Documented here as excluded during pre-screening, before live testing — not a wall discovered mid-benchmark.

---

## 1. Success Rate & Reliability

> Every figure below describes **one benchmark run of 1,000 requested reviews per provider**. None of it is evidence of reliability under repeated trials, concurrent load, or sustained production use — that distinction is preserved throughout, not collapsed into a single "reliability" adjective.

### Apify

- **Requests attempted (committed run, 2026-07-29):** 1 Actor run (`automation-lab/trustpilot`, all 5 domains submitted together) + 34 status-poll requests + 1 dataset-items fetch = 36 total calls (directly counted from `DATA/apify/analysis/timings.json`'s `per_request` array, 35 entries, plus the 1 actor-start call which isn't itself in that array).
- **Successful requests:** all HTTP-level calls succeeded; run status reported `SUCCEEDED`.
- **Raw / unique reviews returned (committed run):** 800 / 800 (target 1,000) — **overall success rate 80%**. `www.thepearlsource.com` returned **0 of 200** requested reviews, with **no error, retry, or failure signal recorded in the benchmark evidence** (the error log is empty).
- 🔴 **Recurring, business-agnostic silent failure — three separate live runs, three different businesses:** an earlier evidence pass (superseded) pinned this same zero-result signature on `www.shein.com` (that run's overall success rate: 79.8%); the committed 2026-07-29 run moved it to `www.thepearlsource.com` (80%, above); a fresh live rerun on 2026-08-13 (Run ID `g2fs1B2BhRm0mDb2R`) moved it again, this time to `www.thehalara.com` (820/1,000, 82% — the other 4 domains, including `www.thepearlsource.com`, returned their full 200 each). Every occurrence reports a clean `SUCCEEDED` status with zero error signal recorded in the benchmark evidence. This is no longer a one-off — it is a repeating defect that lands on a different business each run (79.8%, 80%, and 82% success respectively). With only 3 data points, the evidence does not establish that the affected business is random, or identify what determines it. Root cause remains **unresolved** — would require inspecting Apify's live console run log, not exposed via the documented REST endpoints used here.
- 🔴 **Correction: no duplicates in the committed run.** `DATA/apify/analysis/domain-summary.json` shows `duplicate_records: 0` overall, with `www.shein.com`, `temu.com`, `www.aliexpress.com`, and `thehalara.com` each returning exactly 200 raw = 200 unique = 0 shortfall. A prior draft of this document claimed `aliexpress.com`/`thehalara.com` each had 1 internal duplicate (199 unique) — that claim does not match this evidence file and has been removed.
- **Retries:** 0 triggered (none needed at HTTP level, in any of the three runs).

### OpenWeb Ninja

🔴 **Superseded finding — kept below for provenance, followed immediately by the current state.** An earlier evidence pass recorded a clean run: 50 logical page requests (5 domains × 10 pages, 51 total HTTP calls counting 1 retry), 1,000/1,000 unique reviews (100%), 0 shortfall, 0 duplicates, with exactly 1 HTTP 500 on `www.thepearlsource.com` page 1 (retried once, succeeded). That evidence no longer reflects the account's current state.

**Current, verified state:** a rerun of the identical benchmark to confirm these numbers before publishing failed completely. `DATA/openwebninja/analysis/domain-summary.json`: **0 of 1,000 reviews returned across all 5 domains, `overall_success_rate_percent: 0`**, every domain's `stop_reason: "fetch_failed"`. `DATA/openwebninja/analysis/error-log.json`: **75 entries, every one `status: 429`**, spanning `2026-07-29T08:20:33Z`–`08:57:57Z` (~37 minutes across at least two retry waves) — sustained rate-limiting that the account's own retry/backoff logic could not clear. This was not retested during the 2026-08-13 live-verification pass (no `OPENWEBNINJA_QUOTA_REMAINING` figure was available from the account dashboard — the API has no quota-check endpoint).

- 🔴 **Root-cause investigation (2026-08-13): most consistent with an account/quota-side issue, not a confirmed provider regression — do not read this as proof of permanent product failure.** Compared against the 2026-07-22 successful run: request parameters, endpoint, and auth are byte-identical between the two runs, ruling out a test/configuration difference. The 07-22 run's only friction was one unrelated `HTTP 500`, well clear of any rate limit. The 07-29 failure's **very first logged request was already a 429** — **suggesting, but not proving,** a quota/account state that had already entered before the session started, rather than a limit triggered by this benchmark's own request volume — and persisted through 37 minutes and multiple retry waves despite a 31-minute cool-down pause. The API never exposes rate-limit metadata (`limit`/`remaining`/`retryAfter` are `null` in every logged event, both runs), so the exact mechanism (monthly quota exhaustion on the unpublished "Basic" tier vs. a stricter live limit vs. an account flag) **cannot be confirmed from available evidence** — no status-page incident, changelog entry, or dashboard quota figure was accessible. Treat OpenWeb Ninja's current 0/1,000 result as an accurately-*observed* benchmark outcome, not a settled verdict on the product's underlying reliability.

### DataForSEO

- **Requests attempted:** 10 (5 `task_post` + 5 `task_get`).
- **Successful requests:** 10 of 10, first attempt, 0 retries.
- **Raw / unique reviews returned:** 1,000 / 999 — **99.9% success rate**. The 1 lost review is `temu.com`'s single task returning one review twice within its own response (an internal server-side duplicate, not something the benchmark introduced or re-ran to fix).
- **HTTP errors:** 0. Status codes seen: `20000 "Ok."`, `20100 "Task Created."`.
- **Discrepancies noted:** the secondary source project contains a DataForSEO benchmark script — but that source explicitly labels it "reconstructed... not the original," "unrun this session (no live key)," with an unverified auth mechanism. It should not be assumed to be the original script that produced the raw JSON until its provenance is confirmed one way or the other.

### Outscraper

- **Requests attempted:** 5 submission requests + polling requests until `status: Success`. The two sources disagree on the exact polling count — see "Discrepancies noted."
- **Successful requests:** all attempted requests succeeded; 0 failures, 0 retries (`attempts: 1` throughout).
- **Raw / unique reviews returned:** 1,000 / 1,000 — **100% success rate**, every domain hit its 200-review target on page 1 (`stop_reason: target_reached`).
- **HTTP errors:** 0.
- 🔴 **Polling-request count — corrected.** A prior draft of this document cited "84 poll-phase + 5 submit-phase = 89 total," attributing this to "the canonical `DATA/outscraper/analysis/timings.json`." That 89-request, 512,595ms figure actually belongs to the **archived** `DATA/outscraper/analysis/archive-full-copy/2026-07-21-run/timings.json`. The current, non-archived committed file (`DATA/outscraper/analysis/timings.json`) is a separate, later run (`benchmark_start: 2026-07-29T07:59:24Z`) with **58 poll-phase entries + 5 submit-phase entries = 63 total**, directly counted from its `per_request` array. Both figures are real evidence from this project, just from two different dated runs — see Section 3 for the corresponding timing correction. The primary source's "~120" figure matches neither run's array length and remains **not treated as the fact of record**.

### Lobstr

🔴 **Corrected — same misattribution as Section 3.** These figures were previously labeled an "isolated single-business run" for `www.thepearlsource.com`. They are not: `DATA/lobstr/raw/requests/tasks.json` and `DATA/lobstr/analysis/domain-summary.json` confirm this is the standard **5-domain shared benchmark** (`www.thepearlsource.com`, `www.shein.com`, `temu.com`, `www.aliexpress.com`, `thehalara.com`, 200 each), run on the same shared squid (`8d9e0b80d6d2481283c138104caf022d`) as the archived run — not a separate isolated test.

- **Requests attempted (current, committed run, 2026-07-29):** squid/task/run setup calls (squid-configure, squid-reused check, run creation, 5-task retrieval) + **116 requests from `timings.json`** (15 `poll_run` + 101 `get_results`).
- **Successful requests:** run status `done`, `done_reason: tasks_done` (`run-state.json`); all 101 result pages retrieved, pagination stopped at `no_next_page` after the full target was collected.
- **Raw / unique reviews returned:** 1,000 / 1,000 across **5 domains, 200 each, 0 shortfall on any domain** — **100% success rate**, 0 duplicate_records, 1,000 unique Trustpilot review IDs. (Not "from one Trustpilot business," as previously stated.)
- **HTTP errors: 7** confirmed HTTP 429 "Throttled" events during result-page retrieval — figure itself correct, now correctly attributed to this 5-domain run.
- **Retries:** confirmed directly in `timings.json` — exactly 7 `get_results` entries show `attempts: 2` (94 show `attempts: 1`; 94+7=101), matching the 7 error-log entries 1:1. All 7 succeeded on the second attempt — 0 records lost.
- **A separate, earlier, fully-failed setup attempt exists** (archived, not part of the counted run): an `HTTP 400 DuplicateSquid` error blocked squid configuration before any task or run existed, because the benchmark script tried to rename the clean squid to a name already held by an old contaminated squid. Fixed by never sending a `name` field. *(Already correctly attributed — unchanged.)*
- **The genuinely separate isolated single-business test** (`www.thepearlsource.com` alone) is a different test: first run at a 1,000-review target, later extended to 2,000 reviews on 2026-08-13 (`outputs/lobstr-single-domain-test/summary.json`: 2,000/2,000 unique, 0 duplicates — see header Scope note and Section 4, Scalability). **No request-count, error, or retry evidence for the original 1,000-review version of that isolated test exists anywhere in this project** (`outputs/` is gitignored and was overwritten by the 2026-08-13 rerun) — it must not be conflated with the 5-domain figures above.
- 🔴 **New: five-domain multi-task test (2026-09-04).** A third, separate Lobstr test — all 5 standard businesses as 5 tasks in one dedicated squid (`31973027bc6f482da84b6927647de4d5`), one run (`b1c51b843e6448b89421775ffe368bc3`), target raised to 1,000/business. Result: **5,000/5,000 unique valid reviews, 0 duplicates, 0 unattributed records, all 5 domains exactly 1,000/1,000 raw=valid=unique with 0 shortfall each, 0 HTTP error events (no `errors/event-log.jsonl` file was even created), run status `done` / `done_reason: tasks_done`.** Pagination stopped at `no_next_page` after 6 pages (`page_size=1000`), not a safety-cap cutoff. This is the cleanest run of any Lobstr test in this project — no 429s, no retries, no attribution gaps. Evidence: `data/lobstr/five-domain-multi-task-test-2026-09-04/summary.json`, `run-state.json`, `raw/results/results-page1.json`–`page6.json`.

**Cross-provider reliability summary**

| Provider | Requested | Raw / Unique returned | Success rate | Real errors this run |
|---|---:|---|---:|---|
| 🔴 Apify (`memo23/trustpilot-scraper-ppe`, primary reference as of 2026-09-16) | 5,000 (different test shape — 1,000/business, not 200/business) | Paid plan: 5,000/5,000. Free plan: 50/1,000 (×2, different dates) | **100% on paid plan; 5% on free plan** | 0 every time (no silent-failure pattern observed on either tier) |
| Apify (`automation-lab/trustpilot`, Apify's own dedicated Actor, context) | 1,000 | 800–820 raw / 798–820 unique across 3 runs | 79.8–82% (3/3 runs) | 0 every time (silent zero-result, different business each run: SHEIN → thepearlsource → thehalara.com) |
| OpenWeb Ninja | 1,000 | **0 / 0 (current)** — was 1,000/1,000 in a superseded earlier pass | **0% (current)** | **75× HTTP 429, unrecovered (current)** — was 1× HTTP 500 recovered, superseded |
| DataForSEO | 1,000 | 1,000 / 999 | 99.9% | 0 |
| Outscraper | 1,000 | 1,000 / 1,000 | 100% | 0 |
| Lobstr | 1,000 | 1,000 / 1,000 across 5 domains | 100% | 7× HTTP 429 (all recovered) |

---

## 2. Cost Effectiveness

> Strict distinction maintained throughout: **confirmed billed cost** (from the provider's own billing field) vs. **API usage field** vs. **published rate-card calculation** vs. **credit count with no monetary conversion** vs. **no direct charge observed during the test.** These are never conflated.

### Apify

- **Billing model:** pay-per-event (run-start fee + per-review charge).
- **Published pricing:** $0.005/run start + $0.000575/review (free tier).
- **Measured billed cost: $0.465** — directly from `usageTotalUsd` on the settled Run object (`chargedEventCounts: {start: 1, review: 800}`). This is a real, API-reported billing figure, not a calculation. 🔴 Corrected: the formula estimate for 800 reviews (not 798 — see Section 1's duplicate-count correction) is `$0.005 + 800 × $0.000575 = $0.465` — an **exact match** to the measured cost, not "within 0.26%" as a prior draft stated (that figure used the wrong 798 denominator).
- 🔴 **Corrected: $0.5812**, not $0.5827 — `DATA/apify/analysis/cost-report.json` states `cost_per_1000_successful_reviews_usd: 0.5812` directly ($0.465 ÷ 800 × 1,000). The prior $0.5827 figure used a denominator of 798, which predates this session's duplicate-count correction (the committed run has 0 duplicates and 800 unique reviews, not 798 — see Section 1).
- This is the highest-priced measured figure of the five in dollar terms, but it's a real measured billing figure (from `usageTotalUsd`), not an estimate.
- 🔴 **Platform subscription tiers — corrected 2026-09-09 to match the published article's figures.** Free $0/month ($5 included platform usage), paid tiers starting at $19/month, up to $999/month at the top tier. A 2026-08-19 fetch of `https://apify.com/pricing` had recorded the entry paid tier as "Starter $29/month ($29 usage)" — that figure conflicts with the article's confirmed $19/month starting price (a live pricing-page screenshot as of 2026-09-09) and is superseded; Apify's pricing page may simply have changed between the two checks. This updates/corrects the archived scorecard's looser "Free through Diamond" tier description (`data/apify/reports/archive-full-copy/2026-07-22-run/scorecard.md`). **Important:** this is platform *usage credit*, not a discounted per-review rate — the $0.005/run + $0.000575/review actor pricing above appears to apply uniformly regardless of subscription tier; no evidence anywhere (in this project or on the pricing page) shows a cheaper per-review rate at higher tiers, only a larger monthly credit allowance to spend at the same rate.

### OpenWeb Ninja

- **Billing model:** tiered monthly plans (Free/Pro/Ultra/Mega) + pay-as-you-go, documented per-request.
- **Measured billed cost: none.** The account's actual plan is **"Basic," which is not one of the four published tiers** — no confirmed $/request rate exists for it at all.
- 🔴 **Reconciled 2026-08-19 — this was misattributed, same error pattern as elsewhere in this section.** The figures below trace to the **archived 2026-07-22 successful run** (`data/openwebninja/analysis/archive-full-copy/2026-07-22-run/cost-report.json`: `total_requests_made: 50`, which delivered 1,000/1,000 reviews — so these dollar figures are already, conveniently, $-per-1,000-reviews): **Free $0 (marginal) / Pro $0.125 / Ultra $0.075 / Mega $0.0375 / Pay-as-you-go $0.25.** The **current, committed run's own** `data/openwebninja/analysis/cost-report.json` (2026-07-29, the one that failed) shows different, 10x-smaller figures for a 10x-smaller request count — `total_requests_made: 5`, Free $0 / Pro $0.0125 / Ultra $0.0075 / Mega $0.0037 / PAYG $0.025 — because this run made only 5 logical fetches (one per domain) before failing, not 50. **Both numbers are correctly sourced, to two different runs** — this isn't a data error, it's citing the wrong run's figures as if they applied to the current one. The archived run's figures are the only ones usable as a $/1,000-reviews estimate (by hypothetical tier, not this account's real rate) precisely because that run actually delivered 1,000 reviews to divide by; the current run's 5-request figures have no reviews to amortize against at all (see below) and shouldn't be read as a cheaper current-state cost.
- 🔴 **Current state: 0 successful reviews returned this run** (Section 1) — there is no successful output to amortize any cost against at all, which is a materially worse position than "rate unconfirmed." Whether the 75 failed requests consumed billable account quota **cannot be confirmed from available evidence** — `DATA/openwebninja/analysis/cost-report.json` states OpenWeb Ninja has no account/usage/quota API endpoint, so no before/after quota measurement exists for this run.
- **Cost per 1,000 successful reviews: cannot be computed** — both because the "Basic" plan's rate is unconfirmed and because there were 0 successful reviews this run.

### DataForSEO

- **Billing model:** pay-per-task, priced per 20-review depth block.
- **Published pricing:** $0.00075 per 20 reviews (standard priority) → $0.0075 per 200-review task.
- **Measured billed cost: $0.0375** ($0.0075 × 5 tasks), taken directly from the `cost` field in each `task_post` response — this is a real, per-call billing field, not an estimate. It matches the pre-run formula estimate exactly (0% variance).
- **Cost per 1,000 successful unique reviews: $0.03754** ($0.0375 ÷ 999 × 1,000) — the **cheapest measured figure of all 5 providers**, by a wide margin (Outscraper's calculated estimate is ~72–80x higher for the identical benchmark).
- **Known gap:** DataForSEO has **no cost, error, or pagination report** anywhere in the primary source project's raw outputs — the only DataForSEO cost evidence is the `cost` field embedded in each `task_post`/`task_get` raw response. Any dollar figure above this measured $0.0375 is a published-rate estimate, explicitly labeled as such, never a second measurement.
- 🔴 **Free tier / trial credits, checked 2026-08-19 (`https://dataforseo.com/pricing`):** the pricing page does **not** document a free tier or trial credits for the API itself — it states "you pay only for the individual services you consume" (pure pay-as-you-go) and a **$50 minimum payment** to fund an account. A "Try for free" signup button exists, but its specific terms (amount, duration) aren't detailed on the pricing page itself and weren't investigated further. This resolves the "Free tier / trial credits for evaluation: 0.00/0.2 — not tested" gap in `data/dataforseo/reports/dataforseo-scorecard.md` from "never checked" to "checked, and no such tier is documented" — a materially different, now-confirmed answer, not just filling in the same unknown.
- 🔴 **New 2026-09-11 — trial-credit amount now stated in the published article, not independently re-verified here.** `IMPORTANT/article.md`'s DataForSEO cons list states "**$1 free trial credit**, then $50 minimum top-up." This fills in the specific "Try for free" terms flagged as un-investigated just above — but this project has no independent fetch or screenshot confirming the $1 figure itself (the 2026-08-19 check above explicitly stopped short of it). Treat "$1" as the article's own sourced claim pending a direct citation/screenshot in this project's evidence.

### Outscraper

- **Billing model:** documented per-review rate card (first 100 reviews/period free, then $3/1,000 up to 50,000, $1/1,000 above).
- 🔴 **Subscription-model question resolved 2026-08-19.** A direct fetch of `https://outscraper.com/pricing/` failed (`ECONNREFUSED`) — the second such failure, after an identical block on 2026-08-13 (see `PROOF/external-checks/2026-08-13-pricing-and-ratings.md`). A web search of third-party pricing-review sources (not Outscraper's own page) consistently describes Outscraper as **pure pay-as-you-go with prepaid credits that never expire — no monthly subscription plans, no fixed tiers to commit to.** This confirms the payment *model* (no subscription exists at all), though the third-party sources' specific rate figures (e.g. "first 500 businesses free," "$3/1,000 up to 100,000 then $1/1,000") describe what reads as a different Outscraper product line (general business/places data, not the Trustpilot Reviews endpoint this benchmark uses) and should **not** replace this project's own endpoint-specific, evidence-based figures below (100 reviews/period free, $3/1,000 to 50,000, $1/1,000 above) — only the "no subscription tiers exist" fact is being adopted from that search.
- **Measured billed cost: none.** Outscraper exposes no account-balance or per-request billed-cost endpoint.
- **Calculated/estimated cost:** $2.70 (fresh-account scenario: 900 billable × $0.003) or $3.00 (free-tier-already-used scenario: 1,000 billable × $0.003) — both explicitly labeled calculated, not measured. Measured usage (1,000 reviews across 5 domains) matches the pre-run plan exactly.
- **Cost per 1,000 successful unique reviews:** $2.70–$3.00 (estimated range, not measured) — roughly **72–80x more expensive than DataForSEO's measured cost** for the identical 1,000-review benchmark; this is a documented pricing-model difference, not a measurement error.

### Lobstr

- **Billing model:** credit-based, 1 credit = 1 unique result.
- 🔴 **Correction: the advertised rate is $0.50 per 1,000 reviews, not $1.** Directly fetched from Lobstr's own current Trustpilot Reviews Scraper store page (`https://www.lobstr.io/store/trustpilot-reviews-scraper`, checked 2026-08-13), which states verbatim: "$0.5 per 1000 reviews." A prior draft of this document cited "$1/1,000, writer-confirmed" — that figure does not match the provider's own current published rate and has been corrected. **This makes Lobstr's real cost multiple smaller than previously stated: ~13.3x DataForSEO's measured $0.03754/1,000 (using $0.50), not ~26.6x (the old $1 figure implied).**
- **Measured billed cost in dollars: none directly from this account.** The benchmark account is on Super Admin/"free"-plan terms with no exposed $/credit conversion via the API — the $0.50/1,000 figure is official published documentation, not a billing-API measurement.
- 🔴 **Plan tiers and free-tier allowance, sourced 2026-08-19 (Shehriar, reviewing lobstr.io's pricing page directly — not independently re-fetched in this session):** Free plan = $0/month, 100 credits, 1 slot, 7-day retention. Paid plans start at $20/month (10,000 credits) and scale up to $500/month. This resolves an earlier gap noted in the archived evidence (`data/lobstr/reports/archive-full-copy/2026-07-22-run/scorecard.md`: "Free tier — concept exists but this account's real allowance mechanics under Super Admin terms are opaque") — the allowance was opaque via the API on the benchmark's own test account, not undocumented on the pricing page itself. Per Shehriar, the published $0.50/1,000 rate lines up with the high-volume end of this $20–$500 plan range, not the $20 Starter tier (10,000 credits ÷ $20 works out to $2/1,000, a materially higher effective rate than $0.50/1,000 — consistent with per-credit cost dropping at higher committed tiers, though the exact tier where it reaches $0.50/1,000 wasn't specified).
- **Credits consumed: 1,000** for 1,000 unique reviews (2026-07-29 isolated test) — an exact 1:1 ratio, confirmed by the Run object's `credit_used: 1000` field and before/after credit snapshots (`consumed: 0` → `consumed: 1000`). A follow-up live test on 2026-08-13 pushed this to **2,000 reviews for 2,000 credits** — the same 1:1 ratio held exactly at 2x the volume (see Section 4, Scalability). 🔴 **A third data point, 2026-09-04:** the five-domain multi-task test (5 businesses, one squid, one run) consumed **exactly 5,000 credits for 5,000 unique reviews** — `credit_used: 5000` on the Run object, and `consumed: 0` → `consumed: 5000` on the before/after balance snapshots. The 1:1 ratio now holds at 1,000, 2,000, and 5,000 reviews, and — new here — across multiple businesses aggregated in a single run, not just a single business pushed deeper. Evidence: `data/lobstr/five-domain-multi-task-test-2026-09-04/credits-before.json`, `credits-after.json`, `summary.json`.
- **Cost per 1,000 successful reviews:** 1,000 credits per 1,000 reviews, at the official published rate of $0.50/1,000 — credit consumption is measured; the dollar rate is sourced to the provider's own current documentation, not a billing-API measurement on this account.

**Cross-provider cost summary**

| Provider | Cost evidence tier | Cost per 1,000 successful reviews |
|---|---|---:|
| DataForSEO | ✅ Measured (API `cost` field) | **$0.03754** (cheapest absolute price) |
| 🔴 Apify (`memo23/trustpilot-scraper-ppe`, primary reference as of 2026-09-16) | ✅ Measured (API `usageTotalUsd` field), paid plan only | $0.628 (5-separate-runs config) – $0.658 (single-run config) |
| Apify (`automation-lab/trustpilot`, Apify's own dedicated Actor, context) | ✅ Measured (API `usageTotalUsd` field) | $0.5812 |
| Lobstr | ✅ Credit consumption measured (1:1, confirmed at both 1,000 and 2,000-review scale); dollar rate sourced to official current documentation, not this account's billing API | 1,000 credits at the published $0.50/1,000 = $0.50 |
| Outscraper | ⚠️ Estimated from published rate card only | $2.70–$3.00 (not measured) |
| OpenWeb Ninja | ❌ No confirmed rate for this account's actual plan, and 0 successful reviews this run to amortize against | Not computable |

---

## 3. Speed

> Timing figures are **not directly comparable** across providers without accounting for differing execution models: 🔴 **corrected** — OpenWeb Ninja is the only provider that measures one continuous synchronous run; DataForSEO, Apify, and Outscraper are all async submit-then-poll (create/start, then poll for completion), so their wall-clock totals include polling overhead a purely synchronous call wouldn't have; Lobstr is async with an additional separate paginated result-retrieval phase on top of that.

### Apify

- 🔴 **Correction, 2026-09-09: the 417,896 ms figure is real and belongs to the archived run — the prior "does not appear in any raw evidence file" claim was wrong.** `data/apify/analysis/archive-full-copy/2026-07-22-run/timings.json` states `wall_clock_ms: 417,896` (~6 min 58 s) for that run (798/1,000, SHEIN silent failure — see Section 1). The **current, committed** run (`DATA/apify/analysis/timings.json`, 2026-07-29, thepearlsource silent failure) is a separate run with its own figure, **`wall_clock_ms: 402,134`** (~6 min 42 s). Both are genuine evidence, from two different runs — see the full 4-run breakdown below.
- **Requests (committed run):** 1 actor-start call + 34 status-poll requests + 1 dataset-items fetch = 36 total (the 34+1=35 non-start calls directly counted from `timings.json`'s `per_request` array; matches Section 1).
- **Median latency (committed run):** 2,022 ms (status-poll round trips). **p95 latency:** 2,874 ms. **Min/max:** 779–3,002 ms.
- **Comparability limitation:** the 800/1,000 raw shortfall means duration-per-successful-review is worse than duration-per-requested-review. These figures measure polling-call latency, not per-review fetch time — not directly comparable to OpenWeb Ninja's per-request numbers, the only genuinely synchronous provider (see the corrected execution-model note above).
- 🔴 **New: full 4-run speed breakdown, added 2026-09-09, each figure verified against its own raw `timings.json`/`summary.json` file:**

  | Run | Reviews | Wall-clock | Reviews/min | Source file |
  |---|---|---:|---:|---|
  | Archived 2026-07-22 (SHEIN 0/200) | 798/1,000 | 417,896 ms (6 min 58 s) | 115 | `data/apify/analysis/archive-full-copy/2026-07-22-run/timings.json` |
  | Committed 2026-07-29 (thepearlsource 0/200) | 800/1,000 | 402,134 ms (6 min 42 s) | 119 | `DATA/apify/analysis/timings.json` |
  | Live rerun 2026-08-13 (thehalara 20/200) | 820/1,000 | 326,319 ms (5 min 26 s) | 151 (`reviews_per_minute: 150.77` in file) | `outputs/apify/timings.json` |
  | Single-domain test 2026-08-19 (1,000 requested, capped at 200) | 200/1,000 | 141,316 ms (2 min 21 s) | 85 | `outputs/apify-single-domain-test/timings.json` |

  Actual range: 85–151 reviews/min. Simple average ≈118 reviews/min; review-weighted average (2,618 reviews ÷ 1,287,665 ms combined) ≈122 reviews/min. The variation mainly reflects how many of the 1,000 requested reviews actually came back each run (Apify's silent-failure pattern hitting a different business each time — see Section 1), not a genuine speed improvement.

### OpenWeb Ninja

🔴 **Superseded finding, kept for provenance:** an earlier evidence pass measured 180,749 ms total, 1,870 ms median, 3,684 ms p95 across 50 sequential requests (+1 retry).

**Current, verified state:** `DATA/openwebninja/analysis/timings.json` (final logged wave) shows **67,008 ms wall-clock**, but `error-log.json`'s full span (75 sustained 429s) covers **~37 minutes** across at least two retry waves — the 67s figure only captures the last wave, not the full time spent failing to get data.

### DataForSEO

- **Total wall-clock duration (committed run, 2026-07-21):** 165,513 ms (~2 min 46 s) — the fastest total among the original five.
- **Median latency (per-call round trip, committed run):** 404 ms.
- **p95 latency (committed run):** 2,378 ms, driven by one cold-start `task_post` call.
- **Requests:** 10 total (5 `task_post` + 5 `task_get`).
- 🔴 **Live rerun (2026-08-13) shows materially slower queue processing:** each domain's full submit-to-result cycle took 59–108 seconds this time (thepearlsource 108.3s, shein 92.0s, temu 100.7s, aliexpress 61.3s, halara 58.9s; total ≈421,200 ms serial, vs. 165,513 ms in the committed run — same serial submit-then-poll execution pattern both times, so this is a fair, current-vs-historical comparison, not measurement noise). All 5 domains still completed correctly (1,000/1,000, $0.0375 total, same as the committed run) — this is a **speed regression, not a reliability or cost regression.** Consistent with DataForSEO's own documented worst-case SLA ("up to 45 minutes" for standard priority), which the committed run's lucky fast time never came close to testing.
- **Comparability limitation:** the per-call median latency (404 ms) and p95 (2,378 ms) above were not re-measured during the 2026-08-13 rerun — only the full batch wall-clock was. Treat those two figures as carried forward from the committed run, not re-verified.
- 🔴 **New, added 2026-09-09: aggregate range across the 2 dated runs** (committed 2026-07-21: 999 unique/165,513 ms/362 rpm; live rerun 2026-08-13: 1,000/1,000 unique/≈421,200 ms/142 rpm). **Actual range: 142–362 reviews/min.** Simple average ≈252 reviews/min. Review-weighted average (1,999 reviews ÷ 586,713 ms combined × 60) ≈204 reviews/min. The 2026-08-13 rerun's 1,000/1,000 figure rests on the narrative timing description in this document rather than a separately preserved raw file the way Run 1 has one, so treat it as somewhat less independently verifiable than the committed run's numbers.

### Outscraper

- 🔴 **Corrected figures — the current committed evidence file is a different, later run than what a prior draft cited.** `DATA/outscraper/analysis/timings.json` (`benchmark_start: 2026-07-29T07:59:24Z`) states: **wall-clock 344,297 ms** (~5 min 44 s), **median latency 1,094 ms**, **p95 latency 2,500 ms**, **5 submissions + 58 polling requests = 63 total**. The previously-cited 512,595 ms / 1,169 ms / 1,982 ms / 89-request figures belong to the **archived** `.../archive-full-copy/2026-07-21-run/timings.json` — a real, earlier run, not the current committed evidence.
- **Comparability limitation:** the async submit-then-poll design means total wall-clock reflects many poll cycles, not necessarily slower per-request processing. The current run's faster wall-clock (344,297 ms vs. the archived run's 512,595 ms) came with a *worse* p95 (2,500 ms vs. 1,982 ms) — median improved, tail latency did not; this run should not be read as uniformly faster.

### Lobstr

🔴 **Corrected — misattribution found.** The figures previously here were labeled as an "isolated single-business run" for `www.thepearlsource.com` alone. They are not. `DATA/lobstr/analysis/domain-summary.json` (the file behind the 101-page, 7×429 figures) shows **5 domains, 200 reviews each** — the standard shared benchmark, not the isolated test. Two different runs of that 5-domain benchmark exist in the evidence, with different speed numbers:

- **Current, committed run** (`DATA/lobstr/analysis/timings.json`, 2026-07-29): wall-clock **220,530 ms** (~3 min 41 s), median latency **594 ms**, p95 latency **1,984 ms**, **116 total requests** (15 `poll_run` + 101 `get_results` — this is the same run behind Section 1's 101-page/7×429 figures).
- **Archived run** (`DATA/lobstr/reports/archive-full-copy/2026-07-22-run/`, superseded): wall-clock **99,346 ms** (~1.7 min), median latency **222 ms**, p95 latency **305 ms**, 62 HTTP 429s (all recovered) — required two mid-benchmark bug fixes (`DuplicateSquid`, then a pagination/attribution bug), so its wall-clock is a reconstructed estimate across two invocations. Its own `scorecard.md` explicitly describes this as "5 domains natively handled in one Run" — confirming it is not an isolated single-business test either.
- ❌ **The actual isolated single-business test** (`www.thepearlsource.com` alone, per the header's Scope note) has **no wall-clock, median, or p95 figures recorded anywhere in this project.** `outputs/` is gitignored scratch space and was overwritten by the 2026-08-13 2,000-review rerun before any timing was captured from the original 1,000-review isolated run. The previously-stated **"~179 seconds" does not match any file in the project and has been removed as unverifiable**, not carried forward.
- **Comparability limitation (corrected):** with the isolated test's timing lost, this project currently has **no** speed evidence for Lobstr tested on one business at depth — only on 5 businesses spread thin. This is the opposite of what was previously claimed here.
- 🔴 **New: five-domain multi-task test (2026-09-04).** 5,000 reviews across 5 businesses, one squid, one run: script-measured wall-clock **398,593 ms** (~6 min 39 s), start-to-finish including squid setup and results pagination. The Run object's own `duration` field (platform-measured scrape time only, excluding our script's setup/fetch overhead) reports **321.5 s**. Throughput at the script-measured wall-clock: 5,000 ÷ 398.593 s × 60 ≈ **753 reviews/min** — notably higher than the committed 5-domain run's ≈272 reviews/min (1,000 reviews ÷ 220,530 ms) and the 2,000-review isolated test's ≈359 reviews/min (2,000 ÷ 334,375 ms). **Not proof of true concurrent request handling** (no per-task timing breakdown was captured, so whether the 5 tasks were processed in parallel server-side or just queued efficiently isn't distinguishable from this evidence) — but it is a real, measured throughput improvement at higher aggregate volume, not a regression. Evidence: `data/lobstr/five-domain-multi-task-test-2026-09-04/summary.json`, `run-state.json`.
- 🔴 **New, added 2026-09-09: aggregate range across all 4 dated runs** (archived 2026-07-22: 1,000/99,346 ms/604 rpm; committed 2026-07-29: 1,000/220,530 ms/272 rpm; isolated 2026-08-13: 2,000/334,375 ms/359 rpm; multi-task 2026-09-04: 5,000/398,593 ms/753 rpm). **Actual range: 272–753 reviews/min.** Simple average of the 4 per-run rates ≈497 reviews/min. Review-weighted average (9,000 total reviews ÷ 1,052.844 s combined wall-clock × 60) ≈513 reviews/min. This is the figure published in the article as Lobstr's headline speed range/average, computed here directly from the same 4 underlying runs already documented individually above.
- 🔴 **New: concurrency test (2026-09-04), added 2026-09-09.** A dedicated test compared `concurrency=1` vs. `concurrency=10` on the same 5-domain, 1,000-review shape (`scripts/lobstr/concurrency_test.py`, evidence in `outputs/lobstr-concurrency-test/c1/` and `.../c10/`). `c1`: squid `0f6ac4ffea6b4b5d89b4093ffc51ef55`, run `48d40927b5bf4437b5a9591e7c9bf210` (the same run reused as the API walkthrough example elsewhere in the article), 1,000/1,000 reviews, 0 duplicates, wall-clock **93,609 ms** (1 min 34 s), **641.0 reviews/min**, per-request latency min/median/p95/max **453 / 547 / 1,969 / 1,969 ms**. `c10`: squid `c829a039bc614d12b952400ae5f09611`, run `b05f98e02ab5472c9bd63930552ae4c2`, 1,000/1,000 reviews, 0 duplicates, wall-clock **42,140 ms** (42.1 s), **1,423.8 reviews/min**, latency **485 / 562 / 2,109 / 2,109 ms**. Concurrency 10 was **2.22x faster** (1,424 vs. 641 reviews/min) with per-request latency essentially unchanged — the speedup came from parallel domain-task processing, not faster individual requests. This directly updates the "Concurrency tested: not deliberately tested" note elsewhere in this document (§1, §4) — it was true of the 2,000-review isolated test specifically, but concurrency has since been deliberately tested.
**Cross-provider speed summary**

| Provider | Total wall-clock | Median latency | p95 latency | Execution model |
|---|---:|---:|---:|---|
| DataForSEO | 165,513 ms (committed) / ≈421,200 ms (live rerun, 2026-08-13) | 404 ms (not re-verified live) | 2,378 ms (not re-verified live) | 🔴 Async submit-then-poll (corrected from "Synchronous"), 10 calls |
| OpenWeb Ninja | 67,008 ms (final wave only; ~37 min across all retry waves) | 1,870 ms (superseded) | 3,684 ms (superseded) | Synchronous, sequential, 50+ calls, current run never completed |
| 🔴 Apify (`memo23/trustpilot-scraper-ppe`, primary reference as of 2026-09-16, paid plan) | 182,000 ms (single-run config, 5,000 reviews) / 594,600 ms (5-separate-runs config, same 5,000 reviews) — different test shape (1,000/business) than the other rows | Not separately measured | Not separately measured | Async Actor run + polling; single-run config confirmed faster than Lobstr's equivalent 5,000-review test (398,593 ms) |
| Apify (`automation-lab/trustpilot`, Apify's own dedicated Actor, context) | 402,134 ms (committed run) / 417,896 ms (archived run) / 326,319 ms (live 2026-08-13 rerun) / 141,316 ms (single-domain test) — four separate runs, see full breakdown above | 2,022 ms (committed run) | 2,874 ms (committed run) | Async Actor run + polling |
| Outscraper | 344,297 ms (corrected — a prior draft cited the archived 2026-07-21 run's 512,595 ms) | 1,094 ms | 2,500 ms | Async submit + poll |
| Lobstr | 220,530 ms (current, 2026-07-29) / 99,346 ms (archived 2026-07-22, reconstructed across 2 invocations) — both are the **5-domain** run, not an isolated business | 594 ms (current) / 222 ms (archived) | 1,984 ms (current) / 305 ms (archived) | Async run + sequential retrieval, 5 domains |

---

## 4. Scalability

> The common benchmark did not test real concurrency or 10x volume. Lobstr received one additional isolated scalability test on a single business, verified at **2,000 reviews** (2026-08-13); an earlier 1,000-review version of this same isolated test is referenced elsewhere in this document but has no surviving raw evidence (see the Lobstr subsection below). 🔴 **Updated 2026-08-19:** Outscraper and Apify have now also been pushed past 200 reviews/business on the same isolated single-domain-test pattern (`www.thepearlsource.com`, target 1,000) — both came back **capped at exactly 200**, a confirmed negative result rather than an untested gap (see their subsections below). DataForSEO's 200-review ceiling was already confirmed via documentation. OpenWeb Ninja remains untested past 200 — its account cannot currently clear the 200-review baseline at all (see its subsection).
>
> 🔴 **Updated 2026-09-03:** whether a provider can collect *past* 200 reviews/business was, until now, debated as a Scalability consideration (specifically "volume caps"), and that debate was resolved inconsistently for different providers on 2026-08-19. It's been split out into its own new section, **§4a Data Coverage & Collection Depth**, immediately after this one. §4 below keeps its original, narrower scope — account-level rate limits/concurrency and account-level throughput ceilings.

### Apify

- **Max reviews tested from one business:** 200 in the standard 5-domain benchmark (`maxReviewsPerCompany` set to 200). 🔴 **Updated 2026-08-19 — no longer untested:** an isolated single-domain test set `maxReviewsPerCompany=1000` against `www.thepearlsource.com` (`CONFIRM_PAID_RUN=yes python scripts/apify/single_domain_test.py www.thepearlsource.com 1000`). The Actor run reported `status: "SUCCEEDED"`; the resulting dataset stopped at exactly **200 unique valid reviews, 0 duplicates**, with `pagination_stop_reason: "all_items_downloaded"` — i.e. Apify's own actor considered extraction complete at 200, not the test script cutting it short. Documentation's `maxReviewsPerCompany=0`-means-"unlimited" claim was not tested (a different parameter value); what was tested is that requesting 1,000 does not yield more than 200. Measured cost: $0.12. Evidence: `outputs/apify-single-domain-test/summary.json`, `.../raw/runs/www.thepearlsource.com-run-final.json`, `.../raw/datasets/www.thepearlsource.com-page1-offset0.json`.
- **Concurrency tested:** explicitly `desired concurrency 1` (single-threaded crawl) per the run's own status message — a real, documented value, not merely inferred from timing.
- **Rate-limit events:** 0. HTTP 429 events: 0.
- **10x volume test performed:** No — scorecard states "Degradation at 10x volume: 0.00 — Not tested." (This remains true: the 2026-08-19 test confirms a 200-review ceiling on one business, not a 10x-total-volume or concurrency test.)
- 🔴 **New, added 2026-09-09: alternative-Actor spot-check (2026-09-03), `memo23~trustpilot-scraper-ppe`.** This Actor's own listing claims to bypass Trustpilot's ~200-review limit, so it was spot-checked against `www.thepearlsource.com` (`scripts/apify/alt_actor_single_domain_test.py`, `max_items_requested: 1000` in all 3 runs) as reconnaissance, separate from the primary `automation-lab/trustpilot` Actor tested throughout this document. All 3 runs — the default input, an "all reviews" filter variant, and a "no filter" variant — returned **exactly 50 raw/unique items each**, `run_status: SUCCEEDED`, `pagination_stop_reason: "all_items_downloaded"`, measured cost **$0.05 per run**. So this alternative Actor's real ceiling (50) is *lower* than the primary Actor's confirmed 200-review cap, not higher — it does not deliver on its "bypasses the limit" claim. Evidence: `outputs/apify-alt-actor-single-domain-test/summary.json`, `www.thepearlsource.com-all-summary.json`, `www.thepearlsource.com-nofilter-summary.json`. Each summary is explicitly flagged as "Reconnaissance test only. Field-name schema for this Actor is unconfirmed" — no per-field claims should be made from it. This is a separate reconnaissance finding, not part of any fact tracked for the primary Actor above.
- 🔴 **2026-09-03:** whether the confirmed 200-review ceiling belongs under Scalability (rate limits/concurrency/account-level throughput) or under collection depth was debated on 2026-08-19; resolved by moving collection depth out to its own section — see **§4a Data Coverage & Collection Depth** below for Apify's confirmed cap and its contradiction of the "unlimited" documentation claim.

### OpenWeb Ninja

- **Max reviews tested from one business:** 0 in the current run (target was 200; account could not clear rate limiting to return any reviews at all). A prior, superseded pass had reached 200 (10 pages × 20/page) before this account's current rate-limit state.
- **Concurrency tested:** none — requests were sequential.
- 🔴 **Rate-limit events: 75 confirmed HTTP 429s, none recovered** — this is now direct, current negative evidence of a real scalability failure at low volume (the account couldn't clear rate limiting even at the *standard* 1,000-review test, let alone 10x). Superseded finding: an earlier pass logged 0 real rate-limit events (its one error was an unrelated HTTP 500).
- **10x volume test performed:** No, and moot — the account cannot currently complete the 1x baseline test.
- This run is direct negative evidence on rate limits/concurrency (not merely undocumented), and the practical usable volume is evidently much lower than the published tier suggests.

### DataForSEO

- **Max reviews tested from one business: 200 — a confirmed hard ceiling**, not merely untested further (`pagination_confirmed: false`, no pagination parameters available). Official documentation confirms no `page`/`offset`/`skip`/`cursor` parameter exists for this endpoint — 200 is the documented, current ceiling with no pagination alternative.
- **Concurrency tested:** none (5 sequential tasks).
- **Rate-limit events:** 0. Documented limits (30 POST/min, 100 tasks/POST, 20 `tasks_ready`/min) were never approached (5 POST calls, ~6 `tasks_ready` polls this run).
- **10x volume test performed:** No — "no attempt at 50 tasks concurrently."
- The only provider whose 200-review ceiling is a **proven negative result** (confirmed via documentation) rather than simply an untested gap.

### Outscraper

- **Max reviews tested from one business:** 200 in the standard 5-domain benchmark (never requested more there; every domain hit target on page 1). 🔴 **Updated 2026-08-19 — no longer untested:** an isolated single-domain test requested `limit=1000` against `www.thepearlsource.com` (`CONFIRM_PAID_RUN=yes python scripts/outscraper/single_domain_test.py www.thepearlsource.com 1000`). A single call came back with exactly **200 raw items** (`stop_reason: "partial_last_page"`), even though the same response's own `total_reviews` field reports 17,586 reviews exist for this business — ruling out "the business doesn't have more reviews" as an explanation. Evidence: `outputs/outscraper-single-domain-test/summary.json`, `.../raw/results/www.thepearlsource.com-page1.json`.
  🔴 **Gap closed 2026-08-19 (same day, follow-up call):** a second live call tried `skip=200, limit=200` on the same business to check whether pagination reaches beyond page 1. Response: `status: "Success"`, `data: [[]]` — a genuine empty success, zero reviews, not an error or malformed shape. Zero of 200 requested items were new (0 new, 0 repeated — nothing came back at all). This rules out "the 200-cap is just a per-call `limit` ceiling with more reachable via `skip`" — Outscraper returns nothing past position 200 for this business at all. **Confirmed: this is a real per-business ceiling at 200, the same category of finding as DataForSEO's documented hard ceiling, not merely an untested or partially-open gap.** Evidence: `outputs/outscraper-single-domain-test/skip200-followup.json`, `.../raw/results/www.thepearlsource.com-page2.json`.
- **Concurrency tested:** none.
- **Rate-limit events:** 0. Rate limits are **"not documented at all"** per the provider's own scorecard notes — worse than DataForSEO, which at least publishes explicit numbers.
- **10x volume test performed:** No — not tested this run. This line is corrected from "Pending live pagination test... never executed" to: executed 2026-08-19, both page 1 (`skip=0`) and page 2 (`skip=200`) — confirmed no reviews exist past position 200 for this business, so a 10x-total-volume test isn't reachable via this endpoint for this business regardless.
- 🔴 **2026-09-03:** as with Apify above, the "collection depth past 200" question is covered separately — see **§4a Data Coverage & Collection Depth** below for Outscraper's confirmed cap. Outscraper's actual documented weakness here is that rate limits are "not documented at all" — unaffected by this finding either way.

### Lobstr

- **Max reviews tested from one business: 2,000**, verified (updated 2026-08-13; previously stated as 1,000 — see flag below). A follow-up live test on 2026-08-13, on its own dedicated squid (`a195876effb8446fa4c0171e644595b8`, distinct from the 5-domain benchmark's squid `8d9e0b80d6d2481283c138104caf022d`), targeted 2,000 and returned **2,000/2,000 unique valid reviews, 0 duplicates**, credits consumed measured at exactly 2,000. Run ID `b595fc1fbb3a42aeb77592a12294844c`, wall-clock 334,375 ms, pagination stopped because the requested target was reached (`max_pages_cap`), not a platform-imposed ceiling — no cap was encountered at 2,000. All figures confirmed directly in `outputs/lobstr-single-domain-test/summary.json`.
- 🔴 **Corrected — the "original 1,000-review test" claims below were misattributed, same error as Sections 1 and 3.** An isolated 1,000-review test on `www.thepearlsource.com` is referenced in this document's header (Scope note), but **no raw file for it survives** — `outputs/` is gitignored and was overwritten by the 2026-08-13 2,000-review rerun before its numbers were captured elsewhere. The "7 confirmed HTTP 429 events" previously attributed to it actually come from `DATA/lobstr/analysis/error-log.json`, which belongs to the **5-domain shared benchmark** (confirmed via `tasks.json`'s 5 domain-task mappings), not an isolated single-business run. That 1,000-review isolated test's specific numbers (reviews returned, duplicates, error count) **cannot currently be independently verified from any file in this project.**
- **Concurrency tested:** not deliberately tested in the verified 2,000-review test (result-page retrieval was sequential there). 🔴 **Added 2026-09-09:** a separate, dedicated concurrency test (2026-09-04) since confirmed this deliberately — `concurrency=1` vs. `concurrency=10` on the standard 5-domain/1,000-review shape, concurrency 10 came back **2.22x faster** (1,423.8 vs. 641.0 reviews/min), 1,000/1,000 reviews and 0 duplicates at both settings. See §3 for full figures.
- 🔴 **New: five-domain multi-task test (2026-09-04)** — a different scalability question than the two lines above: not "how deep on one business" but "how much total volume in one squid/one run." All 5 standard businesses added as 5 tasks to **one** squid, target raised to 1,000/business (5,000 run cap), started as **one** paid run: **5,000/5,000 unique reviews, 0 duplicates, 0 unattributed, 0 error events**, run-level `max_unique_results_per_run` cap (5,000) never breached or throttled. This still doesn't test genuine concurrent request handling (no per-task timing breakdown exists — see Section 3) or push any *single* business past 1,000, so it doesn't change the 2,000/2,000 single-business depth finding above. What it adds: confirmed evidence that Lobstr's run-level volume cap scales with task count (5 × 1,000 = 5,000, not capped lower), and the 1:1 credit ratio holds at that aggregate scale too (Section 2). Evidence: `data/lobstr/five-domain-multi-task-test-2026-09-04/summary.json`.
- **10x volume test performed:** Partially, based on the verified 2,000-review test — 2,000 reviews from one business is 10x the standard 200-review-per-business baseline used for the 5-domain comparison, though still not a 10x-total-volume or concurrent-run test. The 2026-09-04 multi-task test is a genuine 5x-*total*-volume test (5,000 vs. the standard 1,000-total benchmark) — the first true total-volume scale test in this project, distinct from the single-business depth test.
- 🔴 **2026-09-03:** the 2,000-review result is really a collection-depth fact — whether the provider can retrieve more than a small, provider-imposed sample per business — not a Scalability-section fact (rate limits/concurrency, degradation-at-10x, volume caps). That's covered in the new **§4a Data Coverage & Collection Depth** below, where Lobstr is the clear leader on this same 2,000/2,000 test.

**Cross-provider scalability summary**

| Provider | >200/business tested? | Concurrency tested? | Real rate-limit evidence? | 10x volume tested? |
|---|---|---|---|---|
| DataForSEO | No — confirmed hard ceiling (negative result) | No | No (0 events) | No |
| Outscraper | **No — confirmed hard ceiling at 200/business**, tested 2026-08-19 (`limit=1000`→200, then `skip=200`→0 new) | No | No (0 events; limits wholly undocumented) | No |
| OpenWeb Ninja | No — the account cannot currently complete even the 200/business standard test (see below) | No | **Yes — 75 real 429s, none recovered** | No |
| Apify | No — confirmed capped at 200, tested 2026-08-19 (`all_items_downloaded` at 200 of 1,000 requested) | Documented `concurrency 1` | No (0 events) | No |
| Lobstr | **Yes — 2,000/2,000 from one business, verified** (2026-08-13; the previously-claimed 1,000-review version has no surviving evidence) | 🔴 **Yes, added 2026-09-09** — `concurrency=1` vs. `concurrency=10` (2026-09-04), 10 was 2.22x faster, 1,000/1,000 reviews at both, 0 duplicates | 0 events at 2,000 (the "7 real 429s" previously shown here belong to the unrelated 5-domain run, not this test) | Partially (2,000 = 10x the 200/business baseline, covered in §4a instead) |

🔴 **2026-09-03:** the bolded collection-depth-past-200 facts above are covered separately in §4a immediately below, not blended into this column.

---

## 4a. Data Coverage & Collection Depth

🔴 **New section, added 2026-09-03.** Whether a provider can actually retrieve more than a small, provider-imposed sample per business — and do so without extra cost or breakage — was previously debated as a Scalability sub-criterion (§4 above) and resolved inconsistently for different providers on 2026-08-19. It's covered as its own section instead, for the reason the article format exists in the first place: it's titled "Best API for Scraping Data at Scale," and whether a provider can deliver more than a toy-sized sample is arguably the central question that title asks, not a footnote under general Scalability.

Facts tracked per provider: confirmed depth beyond the 200-review standard cap · whether a working pagination/retrieval mechanism exists past small page sizes · whether a ceiling was encountered in this benchmark · whether cost/credit consumption stayed linear at higher volume.

### Lobstr
- Confirmed depth: verified **2,000/2,000** unique reviews from one business (2026-08-13), 0 duplicates, no ceiling encountered. This remains a single-business, single-account test, not independently reproduced.
- Pagination mechanism: the undocumented `page_size` parameter (see `data/lobstr/analysis/page_size-parameter-correction.md`) is proven to scale to at least 1,000 items/page with no observed cap; whether it has a ceiling above 1,000 was not tested (this run's full result set was exactly 1,000 per page at the size tested).
- No ceiling encountered: no cap hit at 2,000, the only provider of the five to clear 200.
- Linear cost at scale: exact 1:1 credit-to-review ratio held identically at 1,000 and 2,000 reviews. 🔴 **Reinforced 2026-09-04:** the same 1:1 ratio held a third time at 5,000 credits for 5,000 unique reviews in the five-domain multi-task test (Section 2) — a third data point confirming the same linear pattern.

### DataForSEO
- Confirmed depth: 200 is a documented, current hard ceiling with no pagination alternative.
- Pagination mechanism: none exists; official documentation confirms no `page`/`offset`/`skip`/`cursor` parameter for this endpoint at all.
- No ceiling encountered: a ceiling was directly encountered/documented.
- Linear cost at scale: not applicable; there's no "at scale" case to test.

### Apify
- Confirmed depth: an isolated test requesting `maxReviewsPerCompany=1000` against a real business returned exactly 200, with the Actor's own `pagination_stop_reason: "all_items_downloaded"` — a confirmed ceiling, not a partial result.
- Pagination mechanism: the `maxReviewsPerCompany` parameter exists and is documented to support "unlimited" (0) via automatic pagination "through thousands of reviews," but the observed behavior directly contradicts that documentation at just 5x the standard baseline.
- No ceiling encountered: a ceiling was directly encountered.
- Linear cost at scale: not tested; the run stopped at 200 regardless of the higher request.

🔴 **New 2026-09-11, reconnaissance only — a separate finding from the facts above, about a different Actor.** A separate, third-party Actor, `memo23/trustpilot-scraper-ppe` (not `automation-lab/trustpilot`, the Actor actually tested throughout this benchmark), was tested because its own store page claims to bypass the ~200-review web cap entirely ("No 200-review limit — an iOS mobile-gateway pipeline pulls the full review history where the web stops at ~200"). Two independent live runs against the same business (`www.thepearlsource.com`, 17,586+ reviews on file) — one requesting `maxItems: 1000` (2026-09-03), one requesting `maxItems: 500` (2026-09-11) — both returned exactly **50** reviews (`statusMessage: "Finished — 50 reviews from 1 URL."`, `pagination_stop_reason`/run status both indicate a complete, non-partial result). Changing `maxItems` made no difference; this directly contradicts the Actor's own marketing claim, at a ceiling even further below 200 than the primary Actor's own cap.
  - **Ruled out: this account's Apify free-plan tier is not the cause.** Confirmed via `GET /v2/users/me` that this project's Apify account is on the FREE plan (`isPaying: false`, `plan.id: "FREE"`, $5/month usage credit). But the *same* free-plan account returned 200/business with no ceiling at all from the primary `automation-lab/trustpilot` Actor (Section 1, single-domain test). Since one Actor hit 50 and the other reached 200 on the identical account, the 50-item stop is specific to `memo23/trustpilot-scraper-ppe`'s own logic, not a platform-wide free-account limit.
  - **Billing was inconsistent between the two runs:** the first run (2026-09-03) charged $0 for the 50 dataset items (`chargedEventCounts.apify-default-dataset-item: 0`, only the flat $0.05 actor-start fee), while the second (2026-09-11) charged normally (50 × $0.00075 + $0.05 start = $0.0875). Both runs show `platformUsageBillingModel: "DEVELOPER"` rather than `"USER"`.
  - **Unresolved, needs a paid-plan retest to confirm or refute:** this Actor's own `pricingInfo` metadata documents at least one of its features (`pain-point-analysis`) as gated to Apify accounts "on a paid plan" — so a paid-plan-only 200+/bypass tier is plausible, but untested. Confirming it would require re-running from an Apify account on an actual paid subscription (a recurring monthly cost, not a pay-per-event test), which has not been done in this project.
  - Files: `scripts/apify/alt_actor_single_domain_test.py`, `outputs/apify-alt-actor-single-domain-test/*` (both runs' raw requests/runs/dataset pages and summaries).

🔴 **New 2026-09-16 — the paid-plan retest above was run, with a materially different result. Read this whole entry before citing a number; it resolves the open question directly above, but with important caveats.** This project's Apify account was upgraded to an actual paid plan, and `memo23/trustpilot-scraper-ppe` was retested as a genuine bulk test: all 5 standard benchmark businesses, 1,000-review target each (5,000 total) — the same shape as Lobstr's 2026-09-04 five-domain multi-task test, for direct comparability. Design: **5 separate Actor runs, one per domain** (this Actor's input takes one `startUrls` entry per run — unlike `automation-lab/trustpilot`'s `companyUrls` array — so a single 5-domain run wasn't attempted; guessing at that actor's multi-URL attribution behavior was avoided per this project's "never invent" rule). Script: `scripts/apify/alt_actor_five_domain_bulk_test.py`, run sequentially.

- **Result: 5,000/5,000 unique valid reviews across all 5 domains (1,000/1,000 each), 0 shortfall, 0 duplicates.** Unlike the two free-tier runs above (generic best-effort dedupe against an unconfirmed schema), this run's item schema is now fully confirmed from real sample items (`id`, `url`, `businessUrl`, `rating`, `publishedDate`, `title`/`text`, `reviewerName`, `consumer.id`). All 5,000 items were independently re-validated against these confirmed fields, deduped by the real Trustpilot review `id` (not a hash fallback), and correctly attributed to their source domain via `businessUrl` — 0 duplicate IDs in any domain's dataset, 5,000/5,000 correctly attributed. Evidence: `outputs/apify-alt-actor-five-domain-bulk-test/validated-report.json` (the confirmed-schema figure; supersedes the run's own `summary.json`, which used generic best-effort dedupe and reports the same 5,000/5,000 total by coincidence, not by the same method).
- **Cost, measured directly from `usageTotalUsd` per run:** $3.138 total across the 5 runs ($0.534–$0.69 per domain, no stated reason recorded for the per-domain variance) — **$0.628 per 1,000 reviews**. This is pricier than Lobstr's documented $0.50/1,000 (§2) and higher than a naive extrapolation from this same Actor's earlier $0.05-for-50-items free-tier rate would have suggested (too small a sample to have extrapolated from reliably in the first place).
- **The account-tier dependency flagged as "plausible, but untested" above is now a confirmed, reproducible pattern.** Same Actor, same `www.thepearlsource.com`, same 1,000-item request: **50/1,000 on the free-tier account (2026-09-03) vs. 1,000/1,000 on the paid account (2026-09-16)** — a 20x difference tied directly to billing tier, not to review availability (the business has 17,586+ reviews on file per Outscraper's `total_reviews` field, §4a below). **A paid plan is required to get this Actor's real output** — a materially different cost-of-entry than Lobstr, DataForSEO, or Outscraper, none of which needed a plan upgrade to reach their tested depth.
- **Architecture cost vs. Lobstr's equivalent test:** Lobstr collected the identical 5,000 reviews across the identical 5 businesses in **1 run, 1 squid, 398,593 ms wall-clock** (§3, §4). This memo23 test needed **5 separate paid Actor runs**, **594,600 ms start-to-finish** (sum of the 5 runs' own active durations alone: 516,800 ms) — roughly 49% slower for the same total volume, plus 5 distinct billable run-start fees instead of Lobstr's one run-level charge.
- **Still a reconnaissance finding about a third-party, unscored Actor — not a correction to the scored benchmark.** `automation-lab/trustpilot`, the Actor actually run through Sections 1–6 of this document, remains confirmed hard-capped at exactly 200 reviews/business (entry directly above; §1). memo23 has never been scored on Reliability, Cost, Speed, or Data Quality the way the five primary providers were — this clean 5,000/5,000 run is its only fully successful data point, immediately preceded by one 50-item free-tier run and, before that, another 50-item run under different filter parameters (2026-09-03/2026-09-11). One successful run does not establish the same reliability confidence as Lobstr's three independent successful tests (1,000/1,000, 2,000/2,000, 5,000/5,000 — §4 Lobstr subsection) — see Final Summary for the full weighting of this.
- Evidence: `scripts/apify/alt_actor_five_domain_bulk_test.py`, `outputs/apify-alt-actor-five-domain-bulk-test/summary.json`, `.../validated-report.json`, `.../raw/{requests,runs,datasets}/` (full per-domain request bodies, run objects, poll history, raw dataset pages), `.../{domain}-sample-item.json` (5 files, one per domain, the basis for the confirmed schema above).

🔴 **New 2026-09-16, same day, follow-up — corrects the "5 separate runs" architecture claim directly above; read this before citing that entry's architecture comparison.** The entry above assumed 5 separate runs were necessary because memo23's multi-URL-in-one-run behavior had never been tested and guessing at it risked either silent misattribution or an unexpectedly large bill (if `maxItems` turned out to mean "per business" rather than "total for this run"). That assumption has now been tested directly, deliberately starting at a low, safe `maxItems` value to bound the financial risk of the unknown. Script: `scripts/apify/alt_actor_single_run_multi_domain_test.py`.

- **Test: ONE Actor run, all 5 benchmark businesses in `startUrls` at once, `maxItems: 1000`.** Result: **5,000/5,000 unique reviews, 0 unattributed, every business exactly 1,000/1,000** (`www.thepearlsource.com`, `www.shein.com`, `temu.com`, `www.aliexpress.com`, `thehalara.com`), attributed correctly via `businessUrl`, run status `SUCCEEDED`, `pagination_stop_reason: "all_items_downloaded"`.
- **This settles the `maxItems` semantics question left open above: it is a PER-URL cap, not a total-run cap** — each of the 5 URLs independently got up to 1,000 items, not a shared pool of 1,000 split across all 5. (This also confirms the "worst case ~25,000 items" risk flagged before running was the correct one to guard against, not a false alarm — it simply didn't materialize at `maxItems: 1000` because 5 × 1,000 = 5,000 was itself the intended target here.)
- **memo23 *does* support the same 1-run/multi-business architecture Lobstr uses** — this was previously untested, not disproven, and the untested state was wrongly written up as if 5 separate runs were the only option. That framing is retracted.
- **Speed, measured directly: 142.2s actor run duration, ~182s full script wall-clock** (`startedAt` → `finishedAt` on the Run object, plus polling/dataset-fetch overhead) — this is **faster than Lobstr's equivalent single-run test** (398,593 ms / 398.6s), not slower. The previous entry's "~49% slower" claim is retracted; the corrected comparison is memo23 single-run at ~45–46% of Lobstr's wall-clock for the identical 5,000-review volume.
- **Cost, measured directly from `usageTotalUsd`: $3.29 total, $0.658 per 1,000 reviews** — slightly *higher* per-1,000 than the 5-separate-runs version ($0.628/1,000), and still higher than Lobstr's documented $0.50/1,000, but the cost picture is essentially unchanged; only the architecture and speed claims needed correcting.
- **What this does NOT change:** memo23 is still a separate, unscored, third-party Actor (not `automation-lab/trustpilot`, which remains capped at 200); it still required a paid-plan upgrade (the free-tier account returned only 50/1,000 on the same domain); and this is still a single successful data point, now spread across 2 successful configurations (5-separate-runs and 1-run) rather than 3 independent tests the way Lobstr has. The "track record" and "unscored/paid-plan-dependent" arguments for Lobstr's edge are unaffected by this correction — only the architecture and speed arguments were wrong and are fixed here.
- Evidence: `scripts/apify/alt_actor_single_run_multi_domain_test.py`, `outputs/apify-alt-actor-single-run-multi-domain-test/summary.json`, `.../raw/{requests,runs,datasets}/`.

### Outscraper
- Confirmed depth: hard-capped at 200, confirmed via a same-day `skip=200` follow-up returning a genuine empty result on a business with 17,586 reviews on file.
- Pagination mechanism: a `skip` parameter exists but returned zero new records in the one case tested.
- No ceiling encountered: a ceiling was directly encountered.
- Linear cost at scale: not reached.

### OpenWeb Ninja
The account cannot currently clear the 200-review baseline at all, so a depth-past-200 test is moot on all four facts tracked above.

**Cross-provider Data Coverage summary**

| Provider | Confirmed depth past 200? | Working pagination mechanism? | Ceiling encountered? | Cost stayed linear at scale? |
|---|---|---|---|---|
| Lobstr | Yes — 2,000/2,000, 0 duplicates | Yes — undocumented `page_size` scales to 1,000+/page, no cap found | No — cleared 2,000 with no cap hit | Yes — exact 1:1 credit ratio at 1,000, 2,000, and 5,000 reviews |
| DataForSEO | No — documented 200 hard ceiling | No — no page/offset/skip/cursor parameter exists | Yes — ceiling directly encountered | N/A — no "at scale" case to test |
| Outscraper | No — hard-capped at 200 (confirmed via `skip=200` follow-up) | Minimal — `skip` param exists but returned 0 new records | Yes — ceiling directly encountered | Not reached |
| 🔴 Apify (`memo23/trustpilot-scraper-ppe`, primary reference as of 2026-09-16, **paid account**) | **Yes — 5,000/5,000 across 5 businesses, 0 duplicates**, confirmed both as 5 separate runs and as 1 single run (see entries above) | Yes, a genuine 1-run/multi-business pattern — same shape as `automation-lab/trustpilot`'s `companyUrls` array, confirmed via `startUrls` accepting all 5 URLs at once (`maxItems` behaves per-URL, not as a shared total) | No — cleared 5,000 with no cap hit, on the paid tier only | Reproducibly **tier-dependent, not linear/free**: the identical actor/domain/request returned 50/1,000 on a free account and 1,000/1,000 on a paid account — cost itself ($0.628–$0.658/1,000, measured across the two run configurations) stayed roughly stable across the 5 domains once on the paid tier |
| Apify (`automation-lab/trustpilot`, Apify's own dedicated Actor, context) | No — confirmed capped at 200 (`all_items_downloaded`) | Minimal — `maxReviewsPerCompany` exists but contradicts its own "unlimited" documentation | Yes — ceiling directly encountered | Not tested; stopped at 200 regardless of request size |
| OpenWeb Ninja | Untested — account can't clear the 200 baseline at all | N/A | N/A | N/A |

### Two contenders on collection depth: Lobstr vs. Apify (via memo23)

🔴 **New 2026-09-16.** Across every provider tested in this project, only two have ever been confirmed able to collect past the 200-review standard cap at all: **Lobstr**, and **Apify — but only via the unscored `memo23/trustpilot-scraper-ppe` actor on a paid account**, not via `automation-lab/trustpilot`, the actor actually scored throughout this benchmark (still hard-capped at 200). DataForSEO and Outscraper are both confirmed hard-capped at 200 with no path past it; OpenWeb Ninja cannot even reach 200. So on the specific question "can this provider get more than 200 reviews from one business," this is genuinely a two-horse race, not a five-way comparison — everything below in this subsection compares only those two.

| | Lobstr (2026-09-04) | Apify / memo23, 1-run (2026-09-16) |
|---|---|---|
| Reviews delivered | 5,000/5,000 | 5,000/5,000 |
| Duplicates | 0 | 0 |
| Run architecture | 1 squid, 5 tasks, **1 run** | 🔴 **Corrected: 1 run** — all 5 `startUrls` in a single Actor run (an earlier attempt used 5 separate runs before this was tested; both configurations work) |
| Wall-clock, full test | 398,593 ms (~6 min 39 s) | 🔴 **Corrected: ~182,000 ms (~3 min 2 s)** — **faster than Lobstr**, not slower as previously stated |
| Cost | 5,000 credits = $2.50 at the documented $0.50/1,000 rate (not billing-API-measured on this account) | $3.29, **directly measured** via `usageTotalUsd` |
| Cost per 1,000 reviews | $0.50 (documented rate) | $0.658 (measured, 1-run config; $0.628 in the 5-run config — both measured) |
| Is this the provider's scored/primary product? | Yes — Lobstr's Trustpilot Reviews Scraper is the actor evaluated throughout this whole document | **No** — `automation-lab/trustpilot`, the actor actually scored in §1–§6, is capped at 200; this result comes from a different, third-party community actor |
| Track record backing this result | 3 independent successful tests at increasing scale: 1,000/1,000, 2,000/2,000, 5,000/5,000 | 2 successful configurations of the same 5,000-review test (5-run and 1-run), both on 2026-09-16, immediately preceded by 2 failed/partial attempts on the same actor (50/1,000 twice, under free-tier billing) |
| Repeatability across account tiers | Not tested (same account throughout) | **Confirmed tier-dependent** — 50/1,000 free tier vs. 1,000/1,000 paid tier, same actor, same domain, same request |

🔴 **Corrected 2026-09-16 — the architecture and speed rows above were wrong in the original version of this table and are fixed here; do not carry forward the old "5 separate runs, ~49% slower" claim.** memo23 was re-tested with all 5 `startUrls` in a single run specifically to check whether the earlier "5 separate runs" framing was a real limitation or just an untested assumption. It was the latter: a single run works, delivers the identical 5,000/5,000 result, and completes in **~182s versus Lobstr's 398.6s** — memo23's single-run configuration is measurably *faster*, not slower.

**Why Lobstr still has the edge, even with the architecture/speed correction above:** the review counts tying, and memo23 now also winning on raw single-run speed, still doesn't make these equally strong evidence. Lobstr's result comes from the actual product being evaluated, backed by three separate successful runs at three different scales (1,000 → 2,000 → 5,000) with no failures in between. Apify's matching result required switching away from the actor this whole benchmark actually scores (which remains capped at 200), depends on an unscored third-party actor whose only other data point failed by 95% under different billing conditions, required an account-tier upgrade to work at all, and costs more per 1,000 reviews on a measured basis ($0.658 vs. $0.50) even in its faster, single-run configuration. Speed alone was never the deciding factor for Lobstr's lead — track record, being the actual scored product, and not needing a plan upgrade are, and none of those changed today.

---

## 5. Developer Experience & Usability

🔴 **Merged 2026-08-27.** This category now absorbs what was previously a standalone Section 7, "Input Flexibility & Coverage." Rationale: the writer's editorial review flagged Input Flexibility & Coverage as too thin to stand alone — "Input flexibility fits naturally under something like Usability / Developer Experience." The whole category folds in here rather than being split. Original per-provider input-flexibility facts are preserved below, not discarded.
>
> The merged-in "Input Flexibility & Coverage" bullets in each provider block below are **extracted from each provider's own documented input parameters/endpoints in `api_docs_mcps.txt`** — verifiable documentation facts, not benchmark-run behavior like the rest of this section. No geo/country filter parameter exists for any of the 5 providers as an *input*; `country` only ever appears as an output (reviewer's country) field.

> Only evidence-backed findings are included; adjectives like "easy" or "developer-friendly" are not treated as facts unless a recorded test or log entry supports them.

### Apify

- **Authentication setup:** token-based; worked on the first live attempt.
- **Time to first successful request:** not isolated as a standalone metric (run/poll timing exists, but not framed this way).
- **SDK/client used:** none — raw HTTP, with real retry/error classification logic (401/403/402 → auth/billing error; 400/404 → invalid-request error; 429/500–504 → retryable).
- **Documentation issues:** docs matched live behavior on 4 of 5 domains; the SHEIN discrepancy (Section 1) is a real, unexplained gap between documented "unlimited" capability language and observed zero-result output.
- **Actual implementation problems:** the SHEIN silent zero-result outcome is the standout problem; investigated and inconclusive.
- **Error-message quality:** untested for real failures (none occurred); the client's own error-classification logic was never exercised against a live error.
- **Support interaction:** ❌ Not tested — no support ticket filed.
- Real retry/error-classification code is present in the client, but no error signal was ever surfaced for the SHEIN failure — error-message clarity for real failures remains untested.
- **Accepted input (merged from former §7):** `companyUrls` — domains or full Trustpilot profile URLs, as an array (`api_docs_mcps.txt:161,188`).
- **Endpoint breadth:** 3 — Actor Run, Run Status, Get Dataset Items (`:126,397,457`).
- **Enrichment:** `includeCompanyInfo` flag returns company name/Trust Score/stars/total reviews/categories inline — no separate enrichment endpoint (`:356,509-542`).
- 🔴 **`date` input parameter — missing from `api_docs_mcps.txt`, confirmed live 2026-08-24.** Checked directly against `apify.com/automation-lab/trustpilot/input-schema`: the Actor accepts a `date` preset filter (`last30days` / `last3months` / `last6months` / `last12months`, empty for all dates) in addition to `sort: recency`. `api_docs_mcps.txt` (the project's captured documentation, lines ~155-372) never lists this parameter, and it was never exercised in any benchmark run. The input-flexibility picture below was assessed against that incomplete list, so Apify's Accepted-input/Enrichment facts are understated pending a re-check — see `MISSING_AND_GAPS.md` item B.8.
- Accepts array input (multi-company in one call), single-purpose Actor (one endpoint type), company info enrichment embedded inline. ⚠️ This picture is understated pending the `date`-parameter re-check above.

### OpenWeb Ninja

- **Authentication setup:** API key-based; no friction logged.
- **Time to first successful request:** **0 — no successful request occurred in the current run** (superseded finding: previously "worked, no friction logged").
- **SDK/client used:** none exercised — official SDK examples exist for Shell/Ruby/Node.js/PHP/Python (the broadest official language coverage of any tool tested), unaffected by this run's outcome.
- **Documentation-access limitation, not a doc-vs-behavior gap:** unaffected by this run's outcome — the docs require a real browser to render (a plain HTTP fetch only returns the page shell). Per the project's own `elimination-assessment.md` (E3), this is explicitly recorded as "a minor DX friction point, not a documentation-completeness failure" — every endpoint/param/field matched live behavior exactly once rendered, so there is no actual mismatch between documented and observed behavior.
- **Actual implementation problems — current state:** sustained rate-limiting (75× HTTP 429) that the account's own retry/backoff logic could not clear, across ~37 minutes and multiple waves. Superseded finding: an earlier pass logged only one HTTP 500 with a clean recovery.
- **A genuine positive, still holds:** a real, disclosed dry-run/quota check mechanism exists and was previously demonstrated before any billed calls — a concrete operational-diligence signal, though it did not prevent the current rate-limit exhaustion.
- **Integration complexity:** unaffected by this run's outcome — the `company-reviews` endpoint accepts only `company_domain`.
- **Support interaction:** ❌ Not tested.
- Zero successful requests this run (down from a clean first-request success in the superseded pass); documentation quality and SDK breadth are unaffected by this run's outcome; the 429 error bodies remained clear and structured even in failure.
- **Accepted input (merged from former §7):** `query` — company name or exact domain, search-style (`:3468-3491`).
- **Endpoint breadth:** 2 — Company Search (confirmed) + Company Reviews (`:3468,3495` — the reviews endpoint is itself flagged "Pending live documentation verification" at `:3514`, so this count is not fully confirmed).
- **Enrichment:** none found.
- Broadest endpoint breadth of the five (9 distinct endpoints), plus genuine enrichment (consumer-details, category search). Unaffected by this run's reliability outcome, since this measures documented capability, not this run's result.

### DataForSEO

- **Authentication setup:** API key-based; worked on first request, no setup friction logged.
- **Time to first successful request:** first `task_post` round trip measured at 2,378 ms.
- **SDK/client used:** none — raw HTTP only; official docs provide curl examples only, no SDK tested.
- **Documentation issues:** none logged; every documented parameter/endpoint matched observed live behavior exactly.
- **Actual implementation problems / debugging required:** none logged.
- **Error-message quality:** untested — no real error occurred.
- **Support interaction:** ❌ Not tested.
- **Note on script provenance:** see Section 1 — the primary source project had no benchmark script for this provider at all, meaning the raw logs there could not be traced to producing code. Its provenance relative to the raw evidence should be confirmed before treating it as the verified original.
- Documentation quality and time-to-first-request were both clean; SDK usage and support interaction remain untested.
- **Accepted input (merged from former §7):** `domain` only, a single string (`:1514`).
- **Endpoint breadth:** 3 — `task_post`, `tasks_ready`, `task_get` (`:1486,1686,1692`).
- **Enrichment:** none found.
- Single domain string only (no array batching), one capability (reviews only); a separate search endpoint exists but goes unused. The narrowest input flexibility of the five.

### Outscraper

- **Authentication setup:** API key-based; worked on first real request, "no parser fixes needed" per scorecard notes.
- **Time to first successful request:** ❌ not explicitly logged as a standalone metric.
- **SDK/client used:** none — raw HTTP only; curl examples only in docs.
- **Documentation issues:** none negative — documentation is credited with **proactively flagging real gotchas**: `skip` must be a multiple of 20, empty-result billing behavior, and a ~4-hour result-expiration window. This is more explicit gotcha-flagging than DataForSEO's docs provided.
- **Actual implementation problems:** none logged; the parser's response-shape assumption was verified correct against the very first real response with no fix needed.
- **Support interaction:** ❌ Not tested.
- Authentication and first real request both worked cleanly with no fixes needed.
- **Accepted input (merged from former §7):** `query` — domain or Trustpilot profile URL, as an array, batchable up to 1,000 (`:4622-4645`).
- **Endpoint breadth:** 2 — main endpoint + results/polling (`:4604,4907`). Richest query-parameter set of the five: `limit`, `skip`, `languages`, `sort`, `cutoff`, `fields`.
- **Enrichment:** none found.
- Accepts a domain or URL, batchable up to 1,000 queries per call (real breadth despite narrow endpoint count); no enrichment endpoint found.

### Lobstr

- **Authentication setup:** token-based; worked immediately for every call.
- **Time to first successful request:** ❌ not isolated as a standalone metric — and this provider needed **two fixes at two different stages** before a complete, correctly-attributed dataset was produced (see below): one blocking configuration error in an earlier, separate attempt (before any Run existed), and one silent data-processing bug found only after a Run had already reported success on Lobstr's side. A real friction point the other four providers did not have.
- **SDK/client used:** none — raw HTTP.
- **Documentation issues — one concrete, root-caused finding (a second was retracted 2026-09-01, see below):**
  1. 🔴 **Retracted 2026-09-01:** previously stated the documented maximum results-page `limit` of 100 was silently served as `limit: 10` regardless of what was requested, framed as a Lobstr documentation-vs-behavior gap. Verified live 2026-09-01 that this was a **wrong query parameter name**, not a platform bug: `limit` is accepted by the API but never controlled page size; the real (still-undocumented) parameter is `page_size`, which returned exactly the requested 1,000 items in one page when tested against this same archived run, cross-checked against the run's own reported `total_results`/`total_unique_results`. Lobstr's own documented example request (`api_docs_mcps.txt:2530`) itself uses the non-functional `limit` name, which is presumably how it ended up in this benchmark's client code too. Full evidence: `data/lobstr/analysis/page_size-parameter-correction.md`. Still worth noting as an undocumented-parameter-naming gap — just not "the server silently downgrades your request."
  2. 🔴 **Softened 2026-09-01:** previously framed as "no `task` field at all, requiring a fallback to `company_page_url` for domain attribution" — implying a practical attribution problem. Re-examined directly against live review items (`verify.txt`, matching `data/lobstr/raw/results/results-page1.json`): `company_page_url` is present as a first-class field on **every single review item**, not a fallback — it identifies the source business directly, with no join or matching logic required. The literal fact still stands (no field is literally named `task`), but this is an undocumented-naming gap, not a practical attribution problem — dropping the "requiring a fallback" framing.
- **Actual implementation problems:** an `HTTP 400 DuplicateSquid` error occurred from unconditionally sending a `name` field in a squid-configuration PATCH request, colliding with a pre-existing squid on the account — a bug in the benchmark's own orchestration code, not Lobstr's platform, discovered in an earlier, separate attempt before any task or Run existed. Separately, after a Run had already reported success (1,000/1,000 per Lobstr's own count), the benchmark's local aggregation showed only 200 valid records — a silent shortfall, not a Lobstr-reported error — traced to the `/v1/results` endpoint returning only 10 items/page against the benchmark's `limit=100` requests (🔴 now understood to be the wrong parameter name, not a Lobstr-side cap — see retraction above); the "no `task` field" framing cited alongside this at the time has also since been softened (see documentation issues §2 above) — `company_page_url` was present on every item throughout, so attribution was not actually blocked. Fixed by re-reading the same Run (zero new Runs) after raising the page-count cap and switching attribution to `company_page_url`.
- **Debugging required:** yes — two real bugs found and fixed across this effort (one pre-run blocker, one post-run-success silent data bug), both fully documented with before/after evidence.
- **Error-message quality:** both the `DuplicateSquid` error and the `Throttled` 429 errors returned clear, specific, machine-readable bodies.
- **Support interaction:** ❌ Not tested.
- 🔴 **Note:** this run's Developer Experience was originally assessed against *two* documentation-vs-behavior gaps. Both have since been reassessed: the `/v1/results` page-limit finding was retracted (wrong parameter name, not a platform bug) and the missing-`task`-field finding was softened (`company_page_url` already provides direct attribution on every item — an undocumented naming gap, not a practical one). Neither remaining item is a functional gap.
- **Accepted input (merged from former §7):** full Trustpilot review URL (`https://www.trustpilot.com/review/{domain}`) (`:2382-2396`).
- **Endpoint breadth:** 5 — Create Squid, Add Tasks, Start Run, Run Status, Get Results (`:2234,2378,2459,2481,2512`) — the most granular/multi-step workflow of the five.
- **Enrichment:** none found.
- **`fetch_since` squid param — writer-confirmed real and working (2026-08-20), despite this benchmark never exercising it.** `api_docs_mcps.txt:2334-2342` had flagged `fetch_since` as an assumed/unconfirmed parameter name pending live schema confirmation, and every raw squid-configure capture in `data/lobstr/raw/requests/` shows it sent and echoed back as `null` (never set to a real value or functionally tested in this project). Shehriar confirmed directly that `fetch_since` is a real, working Lobstr feature that lets you request reviews from a specific point in time rather than a fixed record count. Treat the parameter's *existence and function* as writer-confirmed; its behavior under this specific benchmark's Trustpilot squid remains untested.
- Accepts the full Trustpilot review URL; task/run/results workflow; company fields embedded per review (enrichment).

**Cross-provider developer-experience & usability summary**

| Provider | SDK tested? | Real doc-vs-behavior gap found? | Real bug requiring a fix? | Support tested? |
|---|---|---|---|---|
| Apify | No | Yes — SHEIN "unlimited" claim vs. observed 0 results | Unresolved silent SHEIN failure | No |
| Outscraper | No | None (docs proactively flagged real gotchas) | None | No |
| OpenWeb Ninja | No | No — docs require a rendered browser to view, but content matched live behavior exactly (access limitation, not a doc-vs-behavior gap) | **Yes — sustained rate-limiting, unrecovered (current)**, was a transient recovered 500 (superseded) | No |
| Lobstr | No | 🔴 **Both retracted/softened 2026-09-01** — `/v1/results` `limit` "silently capped at 10" finding **retracted** (wrong parameter name, `page_size` is the real one and isn't capped; see `data/lobstr/analysis/page_size-parameter-correction.md`); no `task` field on result items **softened** (`company_page_url` is present on every item, so attribution wasn't actually blocked — undocumented naming, not a functional gap) | Yes — 2 (DuplicateSquid config bug; pagination/attribution bug) | No |
| DataForSEO | No | None found | None | No |

---

## 6. Data Quality & Completeness

> **Ground-truth status for all five providers: validated for exactly 1 of 5 businesses (`www.thepearlsource.com`), a manually-curated 20-row sample.** SHEIN, Temu, AliExpress, and Halara have **no ground truth at all**. Every field-coverage/accuracy figure below is scoped to `www.thepearlsource.com` only and must not be extrapolated to the other four domains.
>
> ⚠️ **Process note:** per the `/knowledge` command, this Data pillar is normally owned by `/datacompare` (run against raw exports in `data/`). That command was not run for this project — the figures below predate it, inherited from the original two-source synthesis. They are not sourced from a `/datacompare` artifact. Re-running `/datacompare` against `data/` before finalizing the article would let these numbers be recomputed/confirmed on the canonical pipeline rather than carried forward as-is.

### Apify

🔴 **This figure is volatile across runs, not a stable characteristic — read the whole entry before citing a number.** An earlier evidence pass (when `www.shein.com` was the run's silent-failure business, and `www.thepearlsource.com` — the only domain with ground truth — succeeded) recorded: 20/20 rows matched (100%) via `authorName`, field coverage 92.3% (12/13, the highest of any tool tested), `company_replied` mismatches on 4/20 rows (same freshness explanation and same 4 authors as OpenWeb Ninja/Lobstr), `owner_reply_date` 0% literal match (a relative-string-vs-absolute-timestamp format artifact, not a data error — the underlying `owner_reply` text matched exactly on all 14 comparable rows), 2 duplicates, 0 missing-field occurrences across 800 returned items.
- **The committed evidence file as of 2026-07-29** (`DATA/apify/analysis/ground-truth-match.json`) reflects a *different* run, where `www.thepearlsource.com` itself was the silent-failure business: **0/20 ground-truth rows matched, `api_items_available: 0`, `field_accuracy: []`** — every row's `reason` is `"no_api_item_with_matching_author_found"`. There is no field-coverage or accuracy figure computable from this evidence.
- **A live rerun on 2026-08-13 moved the failure again**, this time to `www.thehalara.com` — meaning `www.thepearlsource.com` most likely returned data in that attempt too, but this was not re-analyzed against ground truth this session.
- **The actual finding is the instability itself, not either number:** across three live attempts, the one domain with ground truth has swung between full data and zero data unpredictably, because a *different* business fails silently each run. Neither "92.3%" nor "0%" should be read as "Apify's accuracy" — the honest statement is that Apify's ground-truth comparability for this business is not currently reproducible run-to-run.
- **Missing fields (from the evidence-available run):** `review_type` (invited/organic status) has no officially-documented equivalent — a heuristic mapping via `verificationLevel`/`source` was empirically 100% consistent on 19 comparable rows in that pass, but is not a confirmed 1:1 field mapping and cannot be re-checked against the current committed evidence.
- **Duplicates (committed evidence, 2026-07-29 run):** 0 across the 800 returned items — the 2/20 duplicate figure from the earlier evidence-available pass does not apply to this run.
- **Schema consistency (committed evidence):** 0 missing-field occurrences across the 800 items that were returned, per `domain-summary.json`'s `missing_field_counts: {}`.
- 🔴 **Note:** the archived 2026-07-22 run had full ground-truth data available (`thepearlsource` succeeded in that run). The current committed 2026-07-29 evidence is a *different* run where `thepearlsource` returned zero items, making ground-truth comparison inapplicable for this run specifically — though schema consistency is still measurable (0 missing fields across the 800 successful items).

🔴 **New 2026-09-16 — the memo23 alt actor's ground-truth match, computed for the first time.** Everything above is about `automation-lab/trustpilot`, the scored actor. `memo23/trustpilot-scraper-ppe` was never run through this project's ground-truth comparison until now — its paid-tier tests (§4a) returned full, real data for `www.thepearlsource.com` (1,000/1,000, both in the 5-run and 1-run configurations), so there was no longer a reason for this to stay uncomputed. Script: `scripts/apify/alt_actor_ground_truth_compare.py`, run against the single-run test's dataset (`outputs/apify-alt-actor-single-run-multi-domain-test/`), using the identical matching/field methodology as the primary Apify comparison script, adapted only for memo23's own confirmed field names (`reviewerName`, `reviewerCountry`, `companyReply.{text,publishedDate}`, etc.).

- **Ground-truth match: 19/20 (95%)** — 1 unmatched row (`John Kocum`, "Purchase Review"): no reviewer with that name found among the 1,000 returned items, most plausibly because that specific review sits past the 1,000-item depth window, not an extraction error (no other row showed any sign of missing/corrupted data).
- 🔴 **Field coverage: 92.3% (12/13) — corrected same day.** An initial version of this entry claimed "12/12 tracked fields present, nothing absent from memo23's schema," treating this project's own 12-field comparison script as if it were the full checklist. It isn't: every other provider in §6 is scored against a fixed **13**-field checklist, and direct inspection of a raw memo23 item confirms it has **no field literally named or dedicated to `review_type`** (invited/organic status) — the exact same gap already documented for the primary `automation-lab/trustpilot` Actor (§6, 92.3%/12/13). memo23's `verificationLevel`/`source` heuristic reconstructed `review_type` correctly on every comparable row (same as automation-lab's own heuristic), but a working heuristic doesn't count as a dedicated schema field any more than it did for the primary Actor. Corrected figure: **92.3% (12/13)** — tied exactly with Outscraper, not the previously-claimed 100%.
- **Overall field-level accuracy across the 12 present fields: 89.4%.** Perfect (100%) on `author_name`, `author_country`, `rating`, `review_title`, `review_text`, `review_type` (via the heuristic), and `useful_count`; **100% on `owner_reply` text** (14/14 where both sides had a reply).
- **Two mismatched fields, both matching a pattern already established for every other provider in this document, not a memo23-specific defect:** `author_reviews_count` 84.2% (16/19) and `company_replied` 78.9% (15/19) — both explained by the ~2-month capture-time gap between the ground-truth sample (2026-07-21) and this data (2026-09-16): authors post more reviews elsewhere over time, businesses reply to reviews after the fact. `owner_reply_date` shows 0% literal match — the same relative-string-vs-absolute-timestamp format artifact already documented for Apify, Outscraper, and Lobstr elsewhere in this section, not a real accuracy failure (the underlying `owner_reply` text itself matched exactly, see above).
- **Duplicates: 0** (already confirmed in §4a's schema validation pass).
- **In plain terms: memo23's data quality is genuinely solid** — ahead of DataForSEO's 84.6% field coverage, tied exactly with Outscraper's 92.3%, on a 95% ground-truth match rate. This does not change memo23's status as an unscored, third-party actor with a thinner track record than Lobstr (§4a) — it only fills in a previously-uncomputed fact: when memo23 does return data, that data is accurate, at essentially the same schema completeness as Apify's own primary Actor.
- Evidence: `outputs/apify-alt-actor-single-run-multi-domain-test/ground-truth-match.json` (raw field_accuracy array covers the 12 fields this project's methodology checks per-row; the 13th checklist field, `review_type`, is assessed as schema-absent by direct inspection, consistent with how automation-lab's own 12/13 figure was determined elsewhere in this section — not from a field literally present in either script's compared-field list).

### OpenWeb Ninja

🔴 **Superseded finding, kept for provenance:** an earlier evidence pass recorded a 20/20 (100%) ground-truth match via `consumer_name`, 76.9–84.6% field coverage, `company_replied` mismatches on the same 4 authors flagged by Apify/Lobstr, 0 duplicates, 0 missing-field occurrences across 1,000 items.

**Current, verified state:** the account's most recent run returned **0 items across all 5 domains** (Section 1) — there is no data to compute a ground-truth match, field coverage, or schema-consistency figure against at all.

- Field coverage, accuracy, schema consistency, and freshness are all unmeasurable this run for the same reason: no data was returned.

### DataForSEO

- **Ground-truth match rate:** 20/20 (100%), via author name.
- **Field coverage: 84.6% (11/13).**
- **Confirmed mismatches:** 0 on every field both sources actually captured — a clean result, but on a narrower schema than some competitors.
- **Missing fields:** `review_type` (invited-status) and `useful_count` (helpful-vote count) — **neither exists anywhere** in the documented or observed DataForSEO schema.
- **Duplicates:** 1, on `temu.com`, matched on `url` key type (the same duplicate counted in Section 1's reliability figure).
- **Schema consistency:** 0 missing-field occurrences across all 1,000 raw items.
### Outscraper

- **Ground-truth match rate:** 20/20 (100%).
- **Field coverage: 92.3% (12/13).** 🔴 **A 2026-09-03 pass had briefly revised this to 84.6% (11/13), reasoning that `DATA/outscraper/analysis/ground-truth-match.json` only compares 11 of the 13 checklist fields and that `experience_date` is genuinely absent from Outscraper's schema. That revision is reverted 2026-09-09** to match the confirmed, published figure of 92.3% (12/13) — only `review_type` is treated as absent from the schema. (`useful_count` is still a real strength — Outscraper returns `review_likes` as a working equivalent, which DataForSEO lacks entirely; matched 100%, including the one non-zero ground-truth value, MR KEARNEY: 1.)
- **One apparent gap is not real, and shouldn't be held against this score:** `owner_reply_date` also isn't in the ground-truth comparison file, but it genuinely exists in Outscraper's raw output as `owner_answer_date`, with real timestamp values (e.g. `"2026-07-28T21:31:37.000Z"`) — it was simply never wired into the comparison script. That's a benchmark tooling gap, not a data-quality gap, and doesn't count against Outscraper's field coverage.
- 🔴 **Correction: 6 field-level mismatches, not 0.** `DATA/outscraper/analysis/ground-truth-match.json` shows `author_reviews_count`: 18/20 match (2 mismatches) and `company_replied`: 16/20 match (4 mismatches) — a prior draft of this document claimed "the only provider with zero field-level mismatches," which does not match this evidence file. Both mismatched fields follow the **same capture-time-gap pattern already documented for Lobstr**: the ground-truth sample was collected 2026-07-21, this run's evidence is dated 2026-07-29 (an 8-day gap), and both fields are time-varying (authors post more reviews elsewhere over time; businesses reply to reviews after the fact). These are genuine value differences explained by real-world drift between capture dates, not extraction errors — the same reasoning already applied to Lobstr, Apify, and OpenWeb Ninja's `company_replied` mismatches elsewhere in this document.
- **Missing fields (confirmed absent from the schema):** `review_type` (invited/organic) and, as of this correction, `experience_date` — neither has an equivalent anywhere in the documented or observed schema.
- **Duplicates:** 0.
- **Schema consistency:** 0 missing-field occurrences across all 1,000 items.
- 🔴 A 2026-09-03 pass briefly revised field coverage down to 11/13 (84.6%); reverted 2026-09-09 to match the confirmed, published 92.3% (12/13) figure. The 6 field-level mismatches are attributed to capture-timing drift, not an accuracy failure.

### Lobstr

- **Ground-truth match rate:** 20/20 (100%).
- 🔴 **Field coverage corrected 2026-09-03: 100% (13/13), not 92.3% (12/13).** This document previously stated both "92.3% (12/13)" and "none of the 13 ground-truth checklist fields are absent from Lobstr's schema" side by side — a direct contradiction, since those can't both be true. Checked directly against `DATA/lobstr/analysis/ground-truth-match.json`: all 13 fields are present in the comparison, including `experience_date`, which returns real values (e.g. `"2026-07-16T22:00:00Z"`) on every row — it simply has no ground-truth counterpart to check it against (`ground_truth_unavailable`, not missing). The "none missing" statement was the accurate one; the 12/13 figure was stale and is corrected here.
- 🔴 **Confirmed mismatches: 6 total, not 4 — matches Outscraper's pattern exactly.** `DATA/lobstr/analysis/ground-truth-match.json` shows `author_reviews_count`: 18/20 match (2 mismatches) in addition to the `company_replied` mismatch already noted here — this field was previously omitted from this bullet. `company_replied` 4/20 — same 4 authors (Linda B, Human Sun, Sharon R., MW) as Apify and OpenWeb Ninja, now cross-confirmed by **three independent tools**. Both mismatched fields follow the same capture-time-gap pattern documented for Outscraper (ground truth collected 2026-07-21, this run dated 2026-07-29, an 8-day gap) — genuine value drift, not extraction errors. `owner_reply_date` 0/14 literal match — same relative-string-vs-absolute-timestamp format artifact as the other providers; `owner_reply` text itself matched exactly on all 14 rows.
- **Missing fields:** none of the 13 ground-truth checklist fields are absent from Lobstr's schema — confirmed, not just asserted (see field coverage correction above).
- **Duplicates:** 0.
- **Business attribution required a code fix:** result items have no `task` field; attribution uses `company_page_url` matched against the domain list (Section 5).
- **Article field count: 35 data points per review.** This is the canonical figure used in the published article and should be used consistently in article copy, comparison tables, and pros/cons. The raw JSON contains additional internal/pipeline fields, so the raw key count should not be presented as the article's "data points per review" figure.
- **Raw JSON key count:** 40 fields were directly counted in the raw result items. This is an internal raw-schema count and should not replace the article's **35 data points** figure.
- **Separately unresolved:** the secondary source also claims each of the 5 providers' ground-truth checklists actually differ in size (11/11/11/12/13 fields, not a uniform 13-field checklist for all five) — this contradicts the consistent 13-field checklist directly observed in the primary source's own field-coverage files (DataForSEO 11/13, Outscraper 12/13, OpenWeb Ninja 10/13 strict, Apify 12/13, Lobstr 12/13). This narrower disagreement was not re-verified during this pass and is recorded as open, not adopted.
- **Field coverage:** 13/13 (100%, confirmed), with 0 unexplained mismatches and 0 missing fields across 1,000 items. The result was also cross-checked for freshness, with the observed differences attributable to time-varying values rather than extraction failures.

**Cross-provider data-quality summary (www.thepearlsource.com only — no other domain has ground truth)**

| Provider | Ground-truth match | Field coverage | Confirmed mismatches | Duplicates | Missing fields (confirmed absent) |
|---|---|---:|---|---:|---|
| 🔴 Apify (`memo23/trustpilot-scraper-ppe`, primary reference as of 2026-09-16) | **19/20 (95%)** — 1 unmatched (review likely past 1,000-item depth window) | 🔴 **Corrected same day: 92.3% (12/13)**, tied with Outscraper — not 100% as first stated; see full entry above | `author_reviews_count` (16/19) + `company_replied` (15/19), both capture-time-gap drift, not errors | 0 | `review_type` — no dedicated field, same gap as `automation-lab/trustpilot` |
| Apify (`automation-lab/trustpilot`, Apify's own dedicated Actor, context) | **0/20 (current, committed 2026-07-29 evidence)** — volatile, see full entry above | **Not computable this run** (was 92.3% in a superseded pass) | Not computable | 0 (current run) | Not computable |
| OpenWeb Ninja | **0/20 (current)** — was 20/20 in a superseded pass | **Not computable this run** (was 76.9–84.6%) | Not computable | 0 (current run) | Not computable |
| DataForSEO | 20/20 | 84.6% (11/13) | 0 | 1 | `review_type`, `useful_count` |
| Outscraper | 20/20 | 92.3% (12/13) | `author_reviews_count` 2/20 + `company_replied` 4/20 (both freshness, not error — corrected from a stale "0 mismatches" claim) | 0 | `review_type` |
| Lobstr | 20/20 | 🔴 100% (13/13) — corrected 2026-09-03, was 92.3% | `author_reviews_count` 2/20 + `company_replied` 4/20 (both freshness, not error) | 0 | none of the 13 checklist fields |

---

## Final Summary — which platform looks strongest

🔴 **2026-09-11: this section previously ranked providers with a point-based scoring rubric (per-category sub-scores, /10 normalizations, mean-of-N overall totals). That scoring layer has been removed per explicit instruction — this section now states the same underlying conclusion using only the facts and evidence already documented in §1–§6 above, with no point values or rankings computed from them.**

**Caveat that still applies regardless of scoring: the rubric formerly used here (`criteria.md`) is owned by Lobstr's own content team, and Lobstr is one of the five evaluated providers.** Nothing found during this project's audits shows a finding being suppressed or reframed to favor Lobstr — every fact below is independently sourced to raw evidence in §1–§6 — but that ownership fact should travel with any comparison below, not be dropped from it.

**The head-to-head comparison below covers Lobstr, DataForSEO, and Apify** — the three providers with a genuine, defensible differentiator. Outscraper is fully tested and evidenced (see §1–§6) but has no category where it's the clear or sole leader (beaten on Cost and Speed, tied with Lobstr on Data Quality until a correction, capped at 200 like DataForSEO and Apify on Data Coverage) — see FAQ for its full write-up. OpenWeb Ninja is likewise excluded, because its most recent verified run is a total failure (0/1,000 reviews) — see FAQ.

**Which provider leads, by category (facts only, no scoring):**
- **Reliability:** Lobstr and Outscraper both hit 100% (1,000/1,000, 0 duplicates); DataForSEO reached 99.9% (999/1,000, 1 internal duplicate). Apify sits at ~80% across three separate runs, each time from a different business silently returning 0 with a clean `SUCCEEDED` status and no error signal. OpenWeb Ninja's current account state is 0% (75×429, unrecovered).
- **Cost:** DataForSEO has the cheapest *absolute measured price* ($0.03754/1,000, from a real billing field). Lobstr's official published rate is $0.50/1,000 (1:1 credit ratio, confirmed at 1,000/2,000/5,000 reviews). Apify's measured rate is $0.5812/1,000 — pricier than Lobstr, but still a real measured billing figure rather than an estimate. Outscraper's estimated rate is $2.70–$3.00/1,000 (not measured). OpenWeb Ninja's cost is not computable (no confirmed rate for this account's plan, 0 successful reviews to amortize against).
- **Speed:** DataForSEO is the fastest of the five on batch wall-clock (165,513 ms committed run; slowed to ≈421,200 ms on a 2026-08-13 live rerun, still within its own documented worst-case SLA).
- **Scalability (account-level rate limits/concurrency, §4's narrower scope):** DataForSEO and Lobstr both show clean behavior with no real rate-limit events at the standard 1,000-review test. The collection-depth question (past 200/business) is a separate, more consequential fact — see below.
- **Data Coverage & Collection Depth (§4a) — the central differentiator, and genuinely a two-contender question.** Lobstr is the only *scored* provider confirmed able to collect past the 200-review standard cap — verified to 2,000/2,000 on one business, then 5,000/5,000 across 5 businesses in one run, across three independent successful tests. Apify's scored actor, `automation-lab/trustpilot`, is confirmed hard-capped at 200 — its "unlimited via automatic pagination" documentation claim is directly contradicted by observed behavior. 🔴 **New 2026-09-16:** Apify does have a second, unscored path past 200 — the third-party `memo23/trustpilot-scraper-ppe` actor matched Lobstr's exact 5,000/5,000 figure, but only on a paid Apify account (the same actor returned just 50/1,000 on a free account). 🔴 **Corrected same day:** memo23 can also do this in a single run, same as Lobstr — a same-day follow-up test disproved the initial "5 separate runs" framing, and the single-run configuration completed in ~182s, faster than Lobstr's 398.6s. What still holds is the cost gap: 25.6–31.6% pricier per 1,000 reviews (measured, depending on run configuration). So the honest framing is: **on collection depth, it's really Lobstr vs. Apify as the only two contenders that can exceed 200/business at all** — DataForSEO and Outscraper are both confirmed hard-capped, OpenWeb Ninja can't reach 200 in the first place — and between those two contenders, Lobstr's result is backed by a proven, repeatable track record on its actual scored product, while Apify's matching number requires an unscored, less consistent, more expensive substitute for the product actually being evaluated. See the "Two contenders" comparison table in §4a for the full breakdown.
- **Developer Experience & Usability:** Apify has real, working retry/error-classification logic and broad multi-input flexibility (array input, multi-company in one call). DataForSEO's integration is the simplest (single domain string, no SDK, zero doc-vs-behavior gaps). Lobstr required the most integration work of the three (a squid-naming collision — a bug in the benchmark's own script, not Lobstr's platform — plus documentation-vs-behavior gaps, since reassessed as undocumented naming rather than functional bugs).
- **Data Quality:** Lobstr has 100% field coverage (13/13, corrected 2026-09-03 from a stale 92.3%/12/13 figure) with 0 unexplained mismatches. Outscraper is close behind at 92.3% (12/13). DataForSEO sits at 84.6% (11/13). Apify's scored actor (`automation-lab/trustpilot`) has ground-truth comparability that's currently volatile (a different business succeeds or fails silently each run) — no stable figure can be quoted for it. 🔴 **New 2026-09-16, corrected same day:** the separate, unscored `memo23` actor now has a real, computed figure — 95% ground-truth match, **92.3% (12/13) field coverage** (corrected from an initial, wrongly-denominated "100%" claim — tied exactly with Outscraper) — genuinely solid, ahead of DataForSEO's 84.6%. This is a fact about memo23, not about Apify's scored product.

**Strengths & weaknesses, by provider:**

### 1. Lobstr — the strongest overall option

**Strong:**
- The only provider confirmed able to collect past the 200-review standard cap — verified to 2,000/2,000 with no ceiling hit (2026-08-13), and again at 5,000/5,000 unique reviews across 5 domains in a single run (2026-09-04, five-domain multi-task test).
- The only provider with real, naturally-occurring rate-limit evidence in the benchmark's isolated single-business test — all 7 confirmed HTTP 429s recovered cleanly on the immediate retry.
- Exact 1:1 credit-to-review ratio holding at 1,000, 2,000, and 5,000 reviews — zero wasted spend.
- 100% ground-truth match, 0 duplicates, and 100% field coverage (13/13) — a transcription error in an earlier draft of this document had this at 92.3%/12/13, corrected 2026-09-03 after checking directly against the raw evidence. Ahead of Outscraper's 92.3% (12/13) — a narrow lead, not a tie.

**Weak:**
- The most integration work of the three compared here — a squid-naming collision (a bug in the benchmark's own script, not Lobstr's platform) and two documentation-vs-behavior gaps (a documented page `limit` of 100 silently served as 10 — later found to be a wrong parameter name, not a platform cap; result items missing an undocumented `task` field — later found to have direct attribution via `company_page_url` on every item instead).
- Real dollar cost is $0.50/1,000 reviews (corrected from an erroneous $1/1,000 cited in an earlier draft — see §2) — still pricier than DataForSEO's measured $0.03754/1,000, though the gap is half what was previously stated.
- Lobstr owns the scoring rubric (`criteria.md`) formerly used to rank these providers — a disclosed conflict of interest that should temper confidence in any ranking derived from it, even though the underlying facts driving Lobstr's strongest-option conclusion (2,000/2,000 and 5,000/5,000 collected, the other four confirmed capped at or below 200) are independently verified and not in dispute.

### 2. DataForSEO — strongest on cost, no longer the fastest at any cost

**Strong:**
- Cheapest cost of the five, and the only one that's a real *measured* billing figure at that absolute price, not an estimate ($0.03754/1,000 reviews). Reconfirmed by a live rerun (2026-08-13): 5/5 domains, $0.0375 total, identical to the original.
- Zero errors, zero debugging required, docs matched live behavior exactly — reconfirmed live.

**Weak:**
- Confirmed hard ceiling of 200 reviews per business — no pagination parameter exists for this endpoint at all. This was the first *proven* scalability limit found in the whole benchmark. It's at least disclosed upfront in DataForSEO's own documentation, unlike Apify's contradicted "unlimited" claim.
- 🔴 **Speed regression observed live:** a 2026-08-13 rerun took 59–108 seconds per domain to complete, versus 2–3 seconds originally — roughly 2.5x slower under today's queue conditions, though still well within the provider's own documented worst-case SLA ("up to 45 minutes"). Data quality and cost were unaffected; this is a speed-only finding.
- No SDK or support interaction was tested.

### 3. Apify (via `memo23`) — matches Lobstr's depth, but on a paid-only, less-proven third-party Actor

🔴 **Reframed 2026-09-16 to match the published article: `memo23/trustpilot-scraper-ppe` is now the primary Actor this write-up means by "Apify."** `automation-lab/trustpilot` — Apify's own official, dedicated Trustpilot Actor — is documented below as clearly-labeled context: it's the reason a different Actor was needed at all, not the headline figure.

**Strong (memo23):**
- 🔴 On a **paid** account, memo23 matches Lobstr's 5,000-reviews-across-5-businesses figure exactly (5,000/5,000, 0 duplicates, confirmed schema) — in a single run, faster than Lobstr's own (~182s vs. 398.6s). See "Weak" below before treating this as full parity with Lobstr.
- Data quality, computed for the first time 2026-09-16, is genuinely solid — 95% ground-truth match, **92.3% (12/13) field coverage** (tied exactly with Outscraper — missing `review_type` for the same reason `automation-lab/trustpilot` does), 89.4% overall field accuracy — ahead of DataForSEO's 84.6% (§6).
- A real measured billing figure ($0.628–$0.658/1,000, depending on run config), and Apify's broader ecosystem (SDKs, CLI, MCP server) applies to this Actor too.

**Weak (memo23):**
- Requires a **paid-plan upgrade** to work at all — the identical actor/domain/request returned only 50/1,000 on a free account 13 days earlier, a 20x, billing-tier-dependent swing.
- Costs **25.6–31.6% more per 1,000 reviews** than Lobstr's documented rate on a directly measured basis.
- Has exactly **two** successful data points backing it (a 5-separate-runs configuration and a 1-run configuration, both tested the same day, 2026-09-16), immediately preceded by two 50-item shortfalls on the free tier — a much thinner track record than Lobstr's three independent, increasing-scale successes.
- Not Apify's own official product — an unscored, third-party community Actor with less official documentation depth than `automation-lab/trustpilot`.

**For context — `automation-lab/trustpilot`, Apify's own dedicated Actor (this is why memo23 was needed):**
- 🔴 **Confirmed recurring, business-agnostic silent failure across three separate live runs** — SHEIN → thepearlsource → thehalara.com, a different business each time, always a clean `SUCCEEDED` status with zero error signal, always ~80% overall success. Not a one-off anomaly; a repeating defect.
- Requesting `maxReviewsPerCompany=1000` from one business returned exactly 200, with the Actor run itself reporting `SUCCEEDED` / `all_items_downloaded` — a confirmed cap that directly contradicts the documentation's "unlimited via automatic pagination" claim.
- Ground-truth comparability for its one verifiable business is volatile, not a stable characteristic — see §6. Neither a "92%" nor a "0%" figure should be quoted as `automation-lab/trustpilot`'s accuracy without this caveat.
- Real, working retry/error-classification logic in its client, and array-input flexibility (`companyUrls`, multi-company in one call) — genuinely good developer experience, just capped at 200/business.

**On scalability/data coverage, overall:** Lobstr is the only *scored* provider confirmed able to exceed 200 reviews from one business — live-verified at 2,000/2,000 as of 2026-08-13 (an earlier 1,000-review version of this test has no surviving raw evidence), and again at 5,000/5,000 across 5 businesses in one run (2026-09-04). Outscraper and `automation-lab/trustpilot` (Apify's scored actor) have both been tested past 200 as well, and both came back capped at exactly 200 — a confirmed negative result for each, not merely an untested gap (see §4a). DataForSEO's 200 ceiling was already confirmed via documentation. OpenWeb Ninja remains untested past 200, because its account cannot currently clear the 200-review baseline at all. 🔴 **Updated 2026-09-16:** Apify does have a second route past 200 — the unscored `memo23/trustpilot-scraper-ppe` actor, paid account only, matching Lobstr's 5,000/5,000 exactly, in a single run just like Lobstr's own architecture (an initial "5 separate runs" framing was tested and retracted the same day) — making this genuinely **a two-contender question (Lobstr vs. Apify-via-memo23), not a "Lobstr alone" story** on the raw numbers, and not one Lobstr wins on speed either (memo23's single run: ~182s vs. Lobstr's 398.6s). But weighing *how* each number was produced (see the Weak section above and the §4a comparison table), Lobstr remains the stronger, better-evidenced choice: three consistent successful runs on its real scored product, at a lower measured/documented cost ($0.50/1,000 vs. memo23's $0.658/1,000), with no plan upgrade required — versus Apify's two successful configurations (both on the same day) of a different, previously-failing, paid-only third-party actor. DataForSEO and Outscraper still have a confirmed 200-review ceiling with no route past it; OpenWeb Ninja can't reach 200 at all. No provider was tested under real concurrency or at 10x *total* volume across all domains beyond the five-domain bulk tests described here, so the benchmark should not be read as a guarantee of sustained production performance for any provider, including Lobstr.

---

## FAQ

🔴 **Restructured 2026-09-09 to match the published article's FAQ exactly — 4 questions, in the article's order.** The article's FAQ covers OpenWeb Ninja, Outscraper, legality, and official-API pricing; it does not surface rubric scores, category totals, or ranking discussion as standalone FAQ items. Content formerly split across a separate "FAQ / Other Options" section (Outscraper and OpenWeb Ninja write-ups), a much longer FAQ list, and a standalone "Legal FAQ Block" is consolidated into these 4 entries. Nothing was deleted from the project's evidence — the removed Q&As (which API is cheapest/fastest/most reliable/most complete, can any pull past 200 reviews, is Lobstr the best overall choice, is the rubric neutral, does this generalize, why not the official API, is this reproducible, Lobstr's price) restate figures already sourced in full in §1–§6, §4a, the Final Summary, and "Official API Deep-Dive" above — nothing here was uniquely stated only in the old FAQ.

### Why OpenWeb Ninja isn't ranked?

OpenWeb Ninja initially returned 1,000/1,000 reviews with 100% ground-truth accuracy. A later verification run using the **same account** returned **0/1,000 reviews** across all 5 businesses and produced **75 HTTP 429 errors** over roughly 37 minutes. The latest results were too inconsistent to provide a reliable recommendation, so OpenWeb Ninja isn't included in the main Lobstr/DataForSEO/Apify comparison.

🔴 Root-cause note (2026-08-13): this is **more consistent with an account/quota-side issue than a proven provider-wide regression** — the failure starts on the very first request of the session rather than escalating with this benchmark's own traffic, but the API exposes no quota metadata to confirm the exact mechanism. Treat it as an observed benchmark outcome for this account, not a settled verdict on the product. It does retain one genuine structural strength (broadest documented Input Flexibility & Coverage of the five, 9 distinct endpoints, and the broadest official SDK language coverage — Shell/Ruby/Node.js/PHP/Python) but that alone doesn't offset a failed baseline run.

Every fact tracked in this document collapsed as a direct result of the failed rerun (reliability, data quality, cost, speed, and scalability were all clean in the earlier, now-superseded successful pass). Not retested during the 2026-08-13 live-verification pass — the script requires a quota-remaining figure from the account's web dashboard, which has no API endpoint and was not available. The weakest overall result of the five providers, by a wide margin.

### Why isn't Outscraper included in the main comparison?

Outscraper delivered **1,000/1,000 reviews with 0 errors**, plus **92.3% field coverage (12/13) and 20/20 ground-truth matches**. However, it was the **most expensive API tested** (an estimated, not measured, $2.70–$3.00 per 1,000 reviews — roughly 72–80x DataForSEO's measured cost) and it has a **confirmed 200-review-per-business ceiling** (`limit=1000` on one business returned only 200; a same-day `skip=200` follow-up returned zero new reviews, ruling out a simple per-call cap — see §4a). Because of these limitations, it wasn't worth including in the main comparison — it's less competitive for large-scale review collection, even though it's fully tested and evidenced.

Zero real errors of any kind, 100% success rate, reconfirmed by an exact-reproduction live rerun (2026-08-13); documentation proactively flags real integration gotchas (e.g., `skip` must be a multiple of 20); its $3/1,000 rate-card estimate is independently corroborated by the provider's own current published pricing page. It's close behind, not tied with, Lobstr on Data Quality — 92.3% (12/13) vs. Lobstr's 100% (13/13); its 6 field-level *value* mismatches (`author_reviews_count`, `company_replied`) follow the same capture-timing pattern as Lobstr's, not extraction errors. Its own `smoke` test mode requests far more than its docstring advertises (confirmed bug — see `MISSING_AND_GAPS.md` A.7), a real cost surprise for anyone trying it cheaply first. Rate limits are "not documented at all" — the worst-documented limits of the five.

### Is scraping Trustpilot reviews legal?

There isn't a universal yes-or-no answer. **Trustpilot prohibits unauthorized scraping**, while legality depends on the **jurisdiction and how the data is used**. This benchmark tested whether APIs can **technically collect Trustpilot reviews**. Whether that use is legally permitted is a separate question. **Check Trustpilot's terms and applicable laws before using the data commercially. This is not legal advice.**

**Supporting detail:** Trustpilot's Terms of Use for Consumers (Section 4, "Your key responsibilities") state: "You must not access, search or collect content from our platform by any means (automated or otherwise) except as provided on our platform or specifically approved by us," and more specifically: "You must not carry out in any way (including facilitating, permitting or authorising) any text mining, data mining or web scraping of our platform for any purpose without our express permission. This includes the training and development of artificial intelligence systems or models." Source: [Trustpilot Terms of Use for Consumers, Feb 2025, Section 4](https://corporate.trustpilot.com/legal/for-reviewers/terms-of-use-for-consumers/feb-2025). The Business Terms carry the same restriction, differently worded — [Terms of Use and Sale for Businesses, Nov 2025, Section 21 ("Don'ts")](https://corporate.trustpilot.com/legal/for-businesses/terms-of-use-and-sale-for-businesses/nov-2025): "Access, search, or collect content from our platform or services by any means (automated or otherwise) except as permitted" ... "you must not carry out, facilitate, authorise or permit any text or data mining or web scraping in relation to our platform" ... "including the development, training, fine-tuning or validation of artificial intelligence systems or models." No material difference found between the two — both prohibit automated collection/scraping/mining without permission and both explicitly name AI training.

On US legality specifically: not illegal under the CFAA alone. *hiQ Labs v. LinkedIn* (9th Circuit, 2019, reaffirmed 2022 after *Van Buren v. United States* narrowed CFAA in 2021) held that scraping data from a site open to the public does not violate the CFAA's "unauthorized access" prohibition. But LinkedIn separately won on **breach-of-contract and trespass** grounds — a district court found hiQ breached LinkedIn's User Agreement in Nov 2022, hiQ then settled in Dec 2022, paid $500,000, was permanently enjoined from scraping LinkedIn, and shut down. **Net effect: a platform's own Terms of Use (like Trustpilot's clause above) can still create real contract-law exposure even where the CFAA doesn't apply.** [LegalClarity: hiQ v. LinkedIn](https://legalclarity.org/the-final-ruling-in-the-linkedin-scraping-case/) · [Apify: hiQ v. LinkedIn case law](https://blog.apify.com/hiq-v-linkedin/) ⚠️ Jurisdiction scope: this covers US case law only — no EU/French precedent (e.g. the L342-3/Entreparticuliers-style analysis used in lobstr.io's listicle format) was researched for Trustpilot specifically.

### Is the Trustpilot API free?

No. Free, Starter, and Plus plans don't include API access. Premium and Enterprise can add the **API Module** as a paid add-on. Trustpilot doesn't publish API Module pricing, so you'll need to contact its sales team for a quote.

🔴 **Added 2026-09-09, sourced to the published article** — this tier-availability breakdown (Free/Starter/Plus vs. Premium/Enterprise) is new information not otherwise verified in this project's raw evidence. It doesn't contradict "Official API Deep-Dive" above, which notes pricing as unconfirmed for the **Data Solutions API specifically** — that remains true; this answer is about the standard Business API's plan-tier gating, a different (and now confirmed) fact.

---

## Winner Quickstart Facts

Final winner: **Lobstr** — the only provider confirmed able to collect past the 200-review standard cap (see §4a and Final Summary), with a clean reliability record and the highest confirmed field coverage of the five. DataForSEO and Apify are the other two providers in the head-to-head comparison; Outscraper and OpenWeb Ninja are fully evidenced but presented separately — see FAQ. Quickstart facts for the top two, from actual raw captures in this project (no credentials reproduced):

### DataForSEO
- **Auth:** HTTP Basic Auth — `base64(login:password)` sent as `Authorization: Basic {REDACTED}` (built in `SCRIPTS/dataforseo/benchmark.py:54`, formerly `scripts/dataforseo_benchmark.py:50-53` before the 2026-08-13 folder restructure; the header value itself is not present in any raw capture file).
- **Setup:** `POST /v3/business_data/trustpilot/reviews/task_post` with body `{"depth": 200, "sort_by": "recency", "priority": 1}` (from `DATA/dataforseo/raw/task-post/www.thepearlsource.com.json`).
- **Sanitized response fields:** `status_code: 20000`, `tasks[0].id`, `result_count`; retrieved results include e.g. `"title": "The Pearl Source", "rating": {"value": 4.8}, "reviews_count": 17558` (from `DATA/dataforseo/raw/task-get/www.thepearlsource.com.json`).

### Lobstr
- **Auth:** Token auth — `Authorization: Token {REDACTED}` (per `api_docs_mcps.txt:2146-2156`; not present in raw captures).
- **Setup:** `POST https://api.lobstr.io/v1/tasks` with body `{"squid": "...", "tasks": [{"url": "https://www.trustpilot.com/review/www.thepearlsource.com"}]}` (body shape documented in `api_docs_mcps.txt:2380-2397`; the `www.thepearlsource.com` URL itself is confirmed used in `DATA/lobstr/raw/requests/tasks.json`'s `domain_task_map` — that file records the resulting tasks, not the outbound request body).
- **Sanitized response fields:** e.g. `"company_name": "The Pearl Source", "rating_value": 5, "review_body": "...", "stars": 5.0` (from `DATA/lobstr/raw/results/results-page1.json`).

---

## Concept Explainer Choice

⚠️ **Proposed, needs writer confirmation** — the concept most likely to gate the reader's decision in this article: **the self-serve access gate.** Framing: before comparing any third-party API on performance, the reader first needs to understand *why* Trustpilot's own official APIs (including the cross-business Data Solutions API) aren't on the table at all — not because of data scope, but because none of them offer instant self-serve access (criterion E1). Everything that follows in the article is "given that the official route isn't self-serve, which third-party API is actually worth paying for."

Alternative candidate considered: sync vs. async execution models (🔴 corrected — OpenWeb Ninja is the only synchronous provider; DataForSEO, Apify, and Outscraper are all async submit-then-poll (DataForSEO's own scorecard documents a "fully async task model": `task_post` → `tasks_ready` poll → `task_get`); Lobstr is async with an additional separate retrieval phase) — this is real and documented (Section 3, Speed) but reads more like a technical caveat for comparing timing numbers than the single concept that gates the reader's *buying* decision.

---

## Store / Links

| Item | Value | Source |
|---|---|---|
| Lobstr scraper name | "Trustpilot Reviews Scraper" | `api_docs_mcps.txt:2200-2210` (name at 2203; slug `trustpilot-reviews-scraper` at 2206-2210) |
| Lobstr store URL | `https://www.lobstr.io/store/trustpilot-reviews-scraper` — also the direct source for the corrected $0.50/1,000 rate (§2), and already hardcoded correctly in `SCRIPTS/lobstr/benchmark.py:974` (`advertised_rate_per_1000 = 0.5`) even while this document's prose still said $1 | `api_docs_mcps.txt:2956`, `SCRIPTS/lobstr/benchmark.py:382,974` |
| Lobstr CTA link | `https://www.lobstr.io/store/trustpilot-reviews-scraper#cta-link` (standard CTA pattern per `facts.md`/CLAUDE.md) | derived, not independently confirmed |
| Apify docs | `https://apify.com/automation-lab/trustpilot` | `api_docs_mcps.txt:66` |
| DataForSEO docs | `https://docs.dataforseo.com/v3/business_data-trustpilot-reviews-task_post/` | `api_docs_mcps.txt:1449-1450` |
| Outscraper docs | `https://docs.outscraper.com/endpoints/trustpilot-reviews/` | `api_docs_mcps.txt:4568-4569` |
| OpenWeb Ninja docs | `https://www.openwebninja.com/api/trustpilot-company-and-reviews-data/docs` | `api_docs_mcps.txt:3411` |
| Trustpilot official API docs | `https://developers.trustpilot.com/` | research, this session |
| Gist/repo (raw evidence) | `https://github.com/Adamisrail001/trustpilot-api-benchmark` — confirmed live 2026-08-20; `outputs/` is gitignored, everything under `DATA/`/`SCRIPTS/` is public | see Benchmark Methodology Record above |
| Matching no-code listicle | ⚠️ MISSING — writer said ignore for now | — |
| lobstr.io Capterra rating | 5/5 (33 reviews, as of 2026-08-13) | article.md — not independently verified against Capterra by this project; sourced to the published article only |
| DataForSEO Capterra rating | 4.8/5 (15 reviews, as of 2026-08-26) | article.md — not independently verified against Capterra by this project; sourced to the published article only |
| Apify Capterra rating | 4.8/5 (563 reviews, as of 2026-08-26) | article.md — not independently verified against Capterra by this project; sourced to the published article only |

---

## Open Items

1. 🔴 Primary SEO keyword — corrected: Article Meta states this is writer-confirmed ("trustpilot reviews api"), not pending sign-off. Resolved, not open.
2. Matching no-code listicle to cross-link — writer said ignore for now (Store / Links — corrected pointer; no longer in Article Meta)
3. ~~Public gist/repo URL for raw benchmark evidence~~ — resolved 2026-08-20: confirmed live and public at `https://github.com/Adamisrail001/trustpilot-api-benchmark` (Benchmark Methodology Record)
4. Which other tools were discovered pre-benchmark and why excluded — no evidence found anywhere in the project (Eliminations)
5. 🔴 Corrected: the Official API use-cases bullets were removed from Official API Deep-Dive as unnecessary detail — they were already researched and sourced before removal, not an outstanding research gap. Nothing open here.
6. Official API pricing — unconfirmed, no direct billing evidence (Official API Deep-Dive)
8. 🔴 Corrected — Input Flexibility & Coverage no longer exists as a standalone Section 7. Merged into Section 5 ("Developer Experience & Usability") 2026-08-27, per writer instruction that it fit more naturally under Usability/Developer Experience than as its own factor. All 5 providers' input-flexibility facts are now reflected within Section 5's per-provider blocks.
9. 🔴 **Retired 2026-09-11:** this item previously tracked scorecard totals and ranking-methodology changes. All numeric scoring has since been removed from this document (see the header note and Final Summary) — Lobstr is the strongest overall option, DataForSEO and Apify round out the head-to-head comparison, Outscraper and OpenWeb Ninja are fully evidenced but presented separately (see FAQ), all stated qualitatively now with no point totals to track or correct.
18. **New:** Outscraper's committed `DATA/outscraper/analysis/timings.json` is a separate, later run (2026-07-29) from the archived `.../archive-full-copy/2026-07-21-run/` copy this document previously cited as "canonical" — both are real evidence, but a reader pulling from the archived folder will see different timing figures than the current committed file. Worth flagging if either is cited directly in a published article.
10. Concept Explainer Choice ("the self-serve access gate") — proposed, needs writer confirmation
11. 🔴 Legal FAQ — corrected: Trustpilot's *business* ToS has since been researched and quoted (Section 21, "Don'ts"), verified against the live page. Only the EU/French precedent gap remains open — this FAQ still covers US case law only.
12. Lobstr CTA link — derived from the standard pattern, not independently confirmed against the live store page
13. Data Quality & Completeness (Section 6) figures predate `/datacompare` and aren't sourced from its artifact — consider re-running `/datacompare` against `data/` before finalizing
14. ~~Apify's missing Speed and Data Quality figures~~ — resolved 2026-08-13: both filled in from `DATA/apify/analysis/` evidence that already existed (see §3 and §6).
15. **New, unresolved:** Apify's ground-truth comparability for `www.thepearlsource.com` is volatile across runs (a different business fails silently each time) — no stable accuracy/field-coverage figure can currently be quoted for it. Needs either a root-caused fix to the underlying failure or a policy decision on how to report a metric that changes every run.
16. 🔴 **Corrected, still unresolved:** Lobstr's standard 5-domain test does have fresh evidence — a committed run dated 2026-07-29 (7×429 errors, 220,530 ms wall-clock), distinct from the archived 2026-07-22 run (62×429, 99,346 ms) — so figures are not simply "carried forward" from the archived run. See §1 and §3 for the specific misattributions already flagged there.
17. **New, unresolved:** OpenWeb Ninja was not retested during the 2026-08-13 live-verification pass — its current state reflects the last confirmed rerun (prior to this document's correction pass), not a fresh check. A quota-remaining figure from the account's web dashboard would be needed to retest it (no API endpoint exists for this).
19. 🔴 **Retired 2026-09-11:** this item tracked whether `IMPORTANT/criteria.md`/`IMPORTANT/methodology.md` (the separate scoring-rubric files) had been updated to match this document's category changes. Now that all scoring has been removed from `knowledge.md` itself (see header note and Final Summary), this document no longer applies or depends on that rubric at all — `criteria.md`/`methodology.md` remain untouched and out of scope, relevant only if a scored comparison is ever needed again.
20. 🔴 **Retired 2026-09-16:** this item tracked whether `memo23/trustpilot-scraper-ppe` could exceed its observed 50-review free-plan ceiling on a **paid** Apify plan. It has since been tested and confirmed: on an upgraded, paid account, the same Actor returned 5,000/5,000 reviews across all 5 benchmark businesses, in two separate run configurations (5-separate-runs, then a single run with all 5 `startUrls` at once) — see §4a and §6. memo23 is now this document's and the published article's primary reference for "Apify."
