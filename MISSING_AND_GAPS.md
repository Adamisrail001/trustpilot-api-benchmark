# Missing Artifacts & Gaps

Action items only — what still needs to be done, grouped by (A) what the
original brief asked for, and (B) what's needed to make `knowledge.md` fully
accurate. Nothing here has been invented; each item states the gap and the
concrete next step.

---

## A. Against the original brief

1. **DataForSEO has no production-parity script.** The ported
   `scripts/dataforseo_benchmark.py` writes one combined JSON file, not the
   per-domain `task-post`/`task-get` captures or the
   `domain-summary`/`duplicate-report`/`cost-report` outputs the other 4
   providers produce. **To do:** rewrite it using `apify_benchmark.py` as a
   structural template, keeping DataForSEO's actual request/response shape
   (confirmed in `data/raw/dataforseo/`).

2. **DataForSEO is missing 3 analysis files** every other provider has:
   `cost-report.json`, `error-log.json`, `pagination-report.json`. **To do:**
   can only be regenerated by running the fixed script (item 1) against a real
   account.

3. **No `final-verdict.md` for Outscraper or DataForSEO.** **To do:** write
   both, following the format of the other 3 providers' `final-verdict.md`
   files.

4. **Ground truth exists for only 1 of 5 domains** (`thepearlsource`). **To
   do:** manually collect and verify a ground-truth sample for shein.com,
   temu.com, aliexpress.com, and thehalara.com, then extend the 4
   `*_ground_truth_compare.py` scripts (currently hardcoded to thepearlsource)
   to loop over all domains that have one.

5. **No documented rationale for choosing these 5 APIs** over the
   discovered-but-untested Trustpilot Official API (which tied for the highest
   discovery-source coverage) or the other discovered tools. **To do:** either
   test the Official API and the others against `criteria.md`, or write a
   short rationale for the exclusion.

6. **No MCP references exist anywhere** for `api_docs_mcps.txt`'s "mcps" half —
   confirmed absent from both source projects. **To do:** add MCP
   config/manifests here if any get built for these providers later.

7. **Outscraper's `smoke` mode consumes far more than it advertises.** Its
   docstring promises "1 request, `limit=20`, minimal cost," but it actually
   requests `limit=200` (confirmed live: fetched 200 reviews, calculated cost
   $0.30–$0.60). This is a bug in the **original JavaScript** source
   (`outscraper-benchmark.js`'s `runSmoke()` never overrides the limit),
   carried into the Python port unchanged. **To do:** add a real low-limit
   override for the smoke path, or fix the docstring to match actual behavior.

8. **Live write-paths and multi-page pagination remain unverified.** Read-only
   calls were confirmed live for Apify, DataForSEO, and Lobstr; OpenWeb Ninja
   has no free endpoint to test at all. But no script has created a *new* paid
   run/task, and every live test so far resolved on a single page. **To do:**
   run each script with real credentials when ready to spend, and compare the
   fresh `outputs/<provider>/` against `data/*/<provider>/` for consistency.

---

## B. To make `knowledge.md` fully accurate

1. **Rubric ownership conflict:** `criteria.md` is headed "Owner: lobstr.io
   content team," and Lobstr is one of the 5 scored providers. Flagged in
   `knowledge.md` but unresolved. **To do:** independent rubric review, or keep
   this disclosure prominent in any published output.

2. **Ground truth covers only 1 of 5 domains** (same gap as A.4) — every
   accuracy/field-coverage figure in `knowledge.md` §6 is valid for
   `thepearlsource` only and must not be read as representative of the other
   4. **To do:** same as A.4.

3. **Scalability is essentially untested for all 5 providers** — none was
   pushed past 200 reviews/business or tested under real concurrency/10x load.
   DataForSEO's 200-review ceiling is the only *confirmed* (not just untested)
   limit. **To do:** run those tests and update `knowledge.md` §4 with real
   results.

4. **Cost evidence tier varies by provider** — only Apify and DataForSEO have
   a real, API-reported billed-dollar figure; Outscraper's is a published-rate
   estimate, OpenWeb Ninja's actual plan rate is unconfirmed, and Lobstr's
   credit-to-dollar rate is unconfirmed. **To do:** get real billing evidence
   (account dashboards) for the other 3 before treating their costs as equally
   measured in `knowledge.md` §2.

5. **Lobstr's internal number conflicts — resolved.** Its HTTP 429 count (59 vs.
   62) and raw result-item field count (37/39 vs. 40) were re-checked directly
   against both audited source projects' raw files. **59 and 40 are confirmed
   correct** — both are corroborated identically in both projects' raw logs;
   the higher figures (62, and 37/39) don't appear in any raw evidence file in
   either project, only in specific narrative report files. `knowledge.md`
   §1/§6 have been updated accordingly. **Outscraper's polling-request count
   (~120 vs. 84) remains open** — recount directly from
   `data/analysis/outscraper/timings.json` to settle it, then correct
   `knowledge.md` §1/§3.

6. **Apify's `www.shein.com` returned 0/200 reviews with zero error signal**
   anywhere in the evidence — the run itself reported success. Root cause
   unresolved; never reproduced with a second run. **To do:** rerun and/or
   inspect Apify's live console run log (not exposed via the REST endpoints
   this project uses), then update `knowledge.md` §1.

7. **DataForSEO's evidence trail can't yet be independently re-created**
   (depends on A.1/A.2) — until a verified production-parity script exists,
   `knowledge.md`'s DataForSEO figures rest on evidence whose generating code
   is unconfirmed. **To do:** same as A.1.
